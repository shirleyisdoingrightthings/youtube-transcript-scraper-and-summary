.env
output/
__pycache__/
*.pyc
*.pyo
.DS_Store

# logs/ 是已跟踪文件（system_changelog.md 是这套流程的演进史，值得留在版本里），
# 所以这里不能写 logs/ ——写了也不生效，只会让人以为它们没进 Git。

# 本地工具状态 / 临时工作文件（勿提交）
.claude/
err.log
transcript_temp.json
talk_transcript.json
transcript_*.json
# 待用字幕：进 transcripts_pending/ 后会按视频原标题重命名，
# 不再匹配上面的 transcript_*.json，必须单独忽略整个目录
transcripts_pending/

# 播客雷达的本地状态：SQLite 库（二进制、每天增长）、抓取日志、生成的榜单。
# 全部可由 `python3 radar.py poll` 重建，频道白名单 radar_channels.json 才是要进版本的那份。
radar_data/
