- **2026-09-11（四）· 之二（Agent Council 由两角色改三角色；专名核对铁律补五条判据；字幕语种校验补上；主会话推理档回默认）**：经用户确认，把 YC Paper Club 一稿实测走通的「激进 + 保守审稿人并行 → Judge 裁决」流程沉淀为标准协议，替换原有「评审 Agent → 核实 Agent → 主模型协调」两角色机制——原机制的硬伤是"协调"落在生成稿的主模型自己身上，等于自己裁自己；实测三角色能纠正这个偏差（曾纠正一次被误判为"零必改"的复核结论）。**升级模块**：① 新增 `skills/council/`（`progressive.md` / `conservative.md` / `judge.md` 三份可直接传给 `Agent` 工具的 prompt 模板，含模型分配建议——审稿人用中间档、Judge 用最强档——以及"产出必须当场落盘到 `output/<标题>/`，不能只留在对话或临时目录"的强制要求，修复了此前一轮审稿报告因只存在会话临时目录、换会话后被误记为"未交卷"的教训）；② `skills/reader_facing_review.md` 的「Agent Council 终审协议」整段改写为「Agent Council 三角色协议」，补上模型分配表、Judge 失败时不得由主模型代行（区别于审稿人失败可以重跑）、以及"Judge 不通读全文、按定位定点抽取"的读法（修复 Judge 因通读全部材料反复耗光额度失败的问题）；③ `CLAUDE.md` Step 2.5 第 2 条「专名核对铁律」由三句话扩成五条可机械执行的判据（只修听写不补内容 / 拼写修正要有据才能不挂 ⚠️ / 讲者口误保留原话加 ⚠️ 不代改 / 数字冲突按同句内证取值 / ⚠️ 与文末说明一一对应），Step 2.6、Step 4.5 同步改引用新协议；④ `fetch_transcript.py` 的 `fetch_via_io()` 新增拉丁字母占比抽检（`_latin_ratio()`，阈值 60%）与多轨道时优先选 `en` 标注轨道的逻辑，修复付费源曾把非英文自动翻译轨误标成 `en` 静默放行的问题，新增 `tests/test_fetch_language_guard.py`（4 组用例，含"上一个测试文件遗留的 mock 污染共享模块状态"这一测试基建本身的坑，已用 `importlib.reload` 修复并注册进 `tests/run_all.py`）；⑤ `PLAYBOOK.md`、`README.md` 同步更新流程描述与文件树，指向新的三角色协议与 `skills/council/` 目录。**另**：清理了会话环境里指向第三方模型服务商的一处全局配置残留（`~/.claude/settings.json`），并按用户确认把主会话推理档从 `max` 改回默认档——这两项不改动仓库内文件，仅记录于此备查。跑 `tests/run_all.py` 全部 8 组通过。
- **2026-09-11（四）**：**intelligence 译法分两种语境；Notion 版标题与空块规则按用户口径改写**。**升级模块**：`glossary.md` §二（新增 intelligence 一行）、`skills/publication_format.md` §二「排版」与「空行规则」。**触发原因**：Neil Movva 逐字稿搬进 Notion 后，用户评论全篇把 intelligence 译成「智能」读起来别扭，并提了三条 Notion 排版要求；Ratchet Check 提示后用户确认写入。**具体变更**：① intelligence 讲可交付、可买卖的东西时译 AI，讲能力或概念本身时保留「智能」，附判定法与正反例。② 标题文字一律不加粗。③ **所有 `#` / `##` 标题之下都要有一个空块**，包括 `# 嘉宾背景`、`# 核心观点`、`# 写在最后`，取代旧的「非正文标题只空一行」；注明直接用 Notion MCP 改线上稿时空块写作 `<empty-block/>`。④ 顺手消掉一处与既有口径的冲突：旧文写「章节标题一律 `#`，不用 `##`」，但用户 2026-09-02 已允许长节下加 H2，改为「单节过长时可加 `##`，不用 `###`」。⑤ 说话人标签那条补一句效果说明：每轮发言自成一块，相邻两轮之间一个空块，一轮之内可以分多段。
- **2026-09-07（日）· 之二**：**补两条机检覆盖不到的图内失真；一条坐标网格的用法**。**升级模块**：`skills/diagram_style.md` §八（新增两条）、§七（破折号）。**触发原因**：Neil Movva 一稿新增两张配图，四张全部 `✓ 自检通过`，但肉眼看出两处真错。**具体变更**：① **机检只查「文字压文字」与「文字出框」，不查「文字压图形」**——图里画的私家车与公交车盖住了两行结论句，自检照样全绿。已写成硬条目：**有图形元素的图，导出后必须逐格看图形与文字的间距**。这是继调色扩展之后，第二类「机检免疫」的失真，两条都归到 §八「先跑机检，再用眼睛看」之下。② **图内文本一律不用破折号 `——`**：拉丁字体 Architects Daughter 没有中文破折号，掉成两个连字符 `--`，而机检不查字形。f3 两处、f4 结论句一处，改冒号或断句。③ 用户当面提的一条版面判据（同日已先落 f2）：**同一张图里虚线不承担两种以上语义**；坐标网格优先用轴上短刻度，不用满幅虚线——否则和「分组框」「对比边」撞成一片，读者要先分辨虚线是参考还是在说事。

- **2026-09-07（日）**：**导出必须绕开浏览器扩展；修正「把 SVG 拖回 Excalidraw 改」这个查证后不成立的说法**。**升级模块**：`skills/diagram_style.md` §七、§三「为什么不直接调用 Excalidraw」第 4 条。**触发原因**：Neil Movva 一稿重画两张配图时，首轮导出的 PNG 浅填充全被染成饱和深色，`figs/` 里还多出一张 `midnight-lizard-filters.png`；用户随后问导出的图能不能拖回 Excalidraw 继续编辑。**具体变更**：① **§七 新增一条硬规则：`render_figs.py` 一律加 `--no-open`，再用干净浏览器打开脚本打印的地址。** 根因是脚本默认 `open` 系统默认浏览器，而该浏览器装了 Midnight Lizard 类调色扩展，会在导出前把整页重新着色。**最值得记的一点是：几何与对比度机检对此完全免疫、照样全绿**——扩展改的是最终着色，机检读的是坐标与色值属性，抓不到；所以判据只能靠这两条外部症状：`figs/` 里冒出不认识的 PNG（扩展往页面注了自己的 `<svg id>`），以及导出的图比配图页里看到的深。这条补上了「机检覆盖不到的一类失真」，与 §八「先跑机检，再用眼睛看」互为补充——**这次正是靠眼睛而不是机检发现的**。② **§三 第 4 条原来写着「把我们导出的 SVG 拖进 Excalidraw 或 Figma 改」，前半句是错的，已改写。**查证（Exa → excalidraw/excalidraw issue #1924、discussion #6082、#3778）：Excalidraw 把外来 SVG 当图片贴入，整块能挪、不能拆开改，官方明确「把 SVG 转成 Excalidraw 图元至少目前不在本项目范围内」；能重新编辑的只有 Excalidraw 自己导出且勾了 embed scene 的图（场景 JSON 压缩后塞进 SVG metadata），我们的图没有那段数据。改写后的正确路径是 `--svg` 出矢量 → Figma / Illustrator / Inkscape，并补了两个前提（先在本机装 `assets/fonts/` 那两支字体，这些工具通常不吃内联 data-URI `@font-face`；rough.js 的手绘线是细碎 path，能挪能改色但改不出「这是个圆角矩形」，结构性改动仍回配图页改代码）。另注明 Obsidian 的 Excalidraw 插件实现了 SVG 导入后 ungroup，属绕路方案。

- **2026-09-03（十一）**：**自检器补对抗测试，顺带修掉它自己的一个判据 bug；Notion 图片直传实弹验证通过**。**升级模块**：`render_figs.py`（新增 `--selftest`、几何改用户单位）、`skills/diagram_style.md` §八。**触发原因**：用户要求把 diagram-design 能借的东西落进 skill，并实现 Notion 图片直传。**具体变更**：① **上一轮只借了 ADR 0005 的一半**——借了「几何要机检」，没借「检查器必须自带正反两极的用例」。补上 `--selftest`：内置一坏一好两张图，坏图必须报出全部四类、好图必须零告警。② **对抗测试第一次跑就抓出自检器自己的 bug**：好图四条文字全被误报「溢出画布」，因为画布用的是 `getBoundingClientRect()`，而 SVG 没被 CSS 给尺寸时它返回 **0×0**，判据整个失效。**只测坏图的话这个 bug 永远不会暴露**——这正是 ADR 0005 坚持测两极的理由。③ **几何全部改用 `getScreenCTM().inverse()` 折算回 SVG 用户单位**，画布取 `viewBox`：既含全部祖先 transform（`getBBox()` 不含，这是上一轮踩的另一个坑），又与代码里写的坐标同一套数，报错能直接对上源码。两张成品图复验仍全绿。④ **Notion 图片直传：代码此前已存在且完整**（`upload_local_file` / `uploaded_image_block` / `resolve_local_images`，上传端点单独用 `2026-03-11`、建页与查重仍走 `2022-06-28`，multipart 不带 json 头，字节读进内存防重试传空，失败降级成占位文字），`tests/test_notion_images.py` 也已注册进 `run_all.py`（7 组全绿）。**但那些测试注入的是假上传函数，真正的两个端点从没跑过**——本轮做了一次不建页的实弹烟测：660 KB 的 `f1.png` 三步走通，拿到 file_upload id，二次调用命中缓存未重复上传。**上传后的 file_upload 一小时内未被引用即失效，数据库里没留任何东西。**

- **2026-09-03（十）**：**配图导出加机检，借自 diagram-design 的「几何要验证、不能靠评审」**。**升级模块**：`render_figs.py`（新增导出即自检，`--no-lint` 可关）、`skills/diagram_style.md` §八与 §九、配图页四处真 bug 修复。**触发原因**：用户问 [cathrynlavery/diagram-design](https://github.com/cathrynlavery/diagram-design)（⭐30k、MIT、活跃维护）能否借鉴。**具体变更**：① **借的是它的 ADR 0005**——「标签位置用几何校验，不靠人眼评审」，原文那句判据值得抄进脑子：**「只活在散文里的规则，就是会发出坏图的规则」**；他们的教训是九个已发布示例里标签被节点盖住，而所有既有检查全过，因为没有一项读坐标。② **`render_figs.py` 导出时自动跑四类检查**：文字溢出画布、文字戳出所属方框、文字相互重叠、对比度低于 4.5:1（大字 3:1）；有告警即非零退出。③ **第一次跑就在两张「已经手工核过三遍」的图上抓出 4 处真错**：`pill()` 辅助函数的文字沿用了鲜艳描边色（片内 2.91:1、片外 4.02:1），图 2 三处判词的颜色写在 `v2c` 键里、上一轮三档调色的全局替换扫不到（算力接近满载 2.95、时间由内存带宽决定 4.14、利用率回升 2.95）。**手工核对确实漏得很稳。** ④ **记下一个实现坑**：几何要用 `getBoundingClientRect()`，**不能用 `getBBox()`**——后者不含祖先 `transform`，而图身画在 `translate(0,104)` 的 `<g>` 里，第一版用 `getBBox()` 报出 10 处假阳性（顶带标题被算成与图身标签重叠）。⑤ **没借**：38 种编辑风图表类型与模板／皮肤体系（干净线条 + 图标库，与我们的手绘暖纸是两套语言）、动效控制器（我们的产物是导成 PNG 进公众号的静态图）、Mermaid／draw.io 导入链路。⑥ 顺带一条印证：他们的 ADR 专门提到 **CJK 标签会撑出更宽的底板**，阈值从 120 放宽到 200 才盖得住——中文标签更宽不是我们独有的坑。

- **2026-09-03（九）**：**📎 对比表按 SemiAnalysis 口径重做；图 1 补回芯片结构并加一个可算的具体例子**。**升级模块**：图文精读的 `📎 延伸` 区块、配图页图 1（重画）与其图释／aria-label／「读这张图」、`figs/f1.png`。**触发原因**：用户拍板用 SemiAnalysis 那张表重做对比、CS-4 数字转为注；并指出图 1 少了芯片结构，读者看不出「token 的运算如何传导到芯片内部」，要求举一个具体例子，并回答「Transformer 架构又如何影响芯片构造」。**具体变更**：① **对比表重做**——七行（H100／H200／B200／B300／R200／WSE-3／Groq LP30）× 五列（FP8 峰值／片外 HBM 容量·带宽／片上 SRAM 容量·带宽／运算强度），口径统一；**最能说明问题的是运算强度那一列：B300 要 1,688，WSE-3 只要 0.74，差三个数量级**，把嘉宾「权重那部分无可匹敌、KV cache 那部分不行」量化成一行；表下补了两处订正（500 MB 其实是 Groq 的数、HBM 官方是 8 而非约 10 TB/s）。**CS-4 的数转为表后一段注**（该表只到 WSE-3），保留 3 片晶圆／43 PB/s 片上／2.4 Tb/s 片外 I/O／125–135 kW／近 4,000 tok/s／3 与 2 微秒互连，并保留「片上与片外 I/O 差四个数量级」这个题眼与 CS-6 路线。② **图 1 重画成三层**：上层用一个具体例子走一遍（输入「巴黎是法国的首」→ 输出「都」，经 Transformer 80 层，回填箭头绕回输入）；中层把**芯片结构画回来**（封装内两侧各 4 堆 HBM、中间两颗经 NV-HBI 相连的 logic die、die 上 SM 阵列，并标明权重与 KV cache 都住片外）；下层是**数据搬进算力的四级台阶**（HBM 288 GB·8 TB/s → L2 126 MB·20 TB/s → shared memory 228 KB/SM → 寄存器 256 KB/SM → Tensor Core）。③ **加了一个全部由已核数字推出的账**：70B × FP16 = 140 GB 权重，÷ 8 TB/s ≈ 17.5 ms，**单卡上限约 57 token/秒，与峰值算力无关**——这是让「内存受限」从概念变成数字的那一步。④ **在 die 上直接标注因果**（「die 上为什么有 Tensor Core：因为 Transformer 的主要开销就是 MLP 那两次大矩阵乘」），并在正文把四条因果写全：MLP 的大矩阵乘 → Tensor Core；权重巨量但固定 → HBM 堆叠；**KV cache 动态膨胀 → 只能放片外，片上 126 MB 接不住**（晶圆级方案只认一半的根由）；单 die 受光罩上限 → 两颗 die + NV-HBI。⑤ 修掉一处渲染溢出：die 上那个说明框的末两行原来戳出框外。

- **2026-09-03（八）**：**拿到 SemiAnalysis 的芯片对比表，修掉两处不严谨**。**升级模块**：配图页（图 1 存储带补 L2 带宽、底部补运算强度注、出处表加两行）、`figs/f1.png` 重出。**触发原因**：用户提供 `tokenomics.info/cerebras` 的「Cerebras vs Others (chips)」对比表作为出处。**具体变更**：① **更正归属**——该站是 **SemiAnalysis** 的，不是 Cerebras 自己的营销材料；页面明写方法论（ISL/OSL 取自其对 Claude Code、Codex、Cursor 等的内部实测 P50，交互速率以 Artificial Analysis 数据加 Cerebras 公布值为锚点拟合）。定性为**第三方分析**，比厂商自述强，出处表按此标注。② **修掉「片上带宽厂商未公布」这个说法**——此前图上只标容量、正文写「片上比 HBM 快得多」，属于没数的定性断言。该表给出 B200/B300 的片上 SRAM 带宽 **20 TB/s**（H100 是 50 MB · 12.8 TB/s），对 HBM 的 8 TB/s **只有 2.5 倍**，「快得多」明显夸大。图上 L2 现标 `126 MB · 20 TB/s`，正文同步改写。③ **批处理门槛换成实名出处与该表自己的术语**——原写「一份公开的推理性能分析」（外部补入、无法核到），现改为 SemiAnalysis 表中的 **ridgepoint arithmetic intensity**：H100 **591** FLOP/字节（FP8 口径）、按 FP16 约 295，B300 **1688**；权重按 FP16 存时该数约等于所需批量，故门槛在几百量级。④ 图上补一行注：「要把 Tensor Core 喂饱，每读一字节数据得摊到几百次运算；batch = 1 时差着两三个数量级」。⑤ **顺带印证图上两个既有数字**：B300 288 GB / 8 TB/s、Blackwell 片上 126 MB，与该表完全一致；另注意该表里 **Groq LP30 的 SRAM 才是 500 MB**，嘉宾口头说 NVIDIA 逻辑芯片「约 500 MB SRAM」很可能是记混了，图上采用 126 MB 是对的。

- **2026-09-03（七）**：**四张图合成两张，并确立「合并优先、横版优先」两条**。**升级模块**：配图页（三张合成一张 FIG 1，原 FIG 4 成为 FIG 2）、`figs/`（只剩 `f1.png` `f2.png`）、图文精读正文配图 4 处 → 2 处、`skills/diagram_style.md` §一。**触发原因**：用户否掉竖版方案，理由是**读者是往下滑的**——一张滑很久滑不到底的图会耗掉耐心，而横版的篇幅一眼可预期，读者能自己决定点不点开；并指出真问题是**逻辑结构**而非朝向（「一个 token 走一圈就该画成循环」），以及**一节放四张图会导致后面每节通货膨胀**，能一张理清就一张。**具体变更**：① **三张合一**——原「token 循环 / 芯片解剖 / 运算与硅片对应」其实是同一条链的三段，合成一张：上半是**真正画成循环**的六步（回填箭头绕回第一步），②–⑤ 套在虚线容器里标 `× N 层`；下半是一条从远到近的存储带（HBM 288 GB · 8 TB/s → L2 126 MB → 寄存器 256 KB · shared memory 228 KB/SM），**六步各引一条虚线汇成一路、指向 HBM**，底下一根横贯箭头写「每生成一个 token 都要从左搬到右一次」。信息一点没少，图少了两张。② **修了一处逻辑硬伤**：原对应图把六步画成平铺清单，**掩盖了 ②–⑤ 每层重复 N 次、而 ① 与 ⑥ 每个 token 只做一次**这件事；新图用虚线容器把这条画了出来。③ **芯片解剖那张整张退役**，双 die / NV-HBI / SM 阵列属于 Level 3 细节，合并后只保留存储带上的容量与带宽数字——顺带消掉了 die 数画错的风险面。④ **竖版试做作废**（试做件留在临时目录）：结论是**先靠合并与减量降密度，再谈放大字号，不要靠拉长画布换字号**；器件类图更不能转竖版，芯片本来就是横的。⑤ **正文配图 4 → 2**，图注改为不编号的「图 · ⋯」，让每张图跟着自己的论证段落走，不必为编号顺序迁就位置。⑥ 发现页面被外部改过：FIG 4 正文多了一句批处理门槛（「H100 上约 280 个 token」），**保留其判断但把出处从「一份公开的推理性能分析」改成可自行核算的口径**（峰值算力 ÷ 内存带宽 = 每字节需摊到的运算次数，H100 SXM 按 FP16 峰值与 3.35 TB/s 算出约 300，对应批量在几百量级）。

- **2026-09-03（六）**：**文字对比度全面返工——语义色从两档改三档**。**升级模块**：配图页调色板与全部文字色、`figs/*.png` 重出、`skills/diagram_style.md` §三 颜色表与字号层级。**触发原因**：用户反复反馈「图上文字太浅、不醒目」，对照 Akshay 的图确实差一截。**具体变更**：① **先量后改**——写脚本算了相对纸底 `#F7F2E4` 的 WCAG 对比度，**六项里五项不达标**：注记 `#857F72` 3.6:1、解读句 `#6C7FD8` 3.3:1、虚线容器 `#B3A98F` 2.1:1、**橙字写在橙底上只有 2.9:1**、蓝字在蓝底 4.0:1（正文小字线是 4.5:1）。「字太浅」是有数的，不是感觉。② **根因是把描边色当字色用**——语义色原本只有「描边 + 填充」两档，写字直接拿描边那支，而鲜艳的描边色天生不够黑。改成**三档：鲜艳描边 / 很浅填充 / 深字色**（橙 `#E8590C` / `#FFE3C7` / `#A03C08`，蓝 `#1971C2` / `#D3E9FA` / `#12558F`，玫红 `#C2255C` / `#FBD9E4` / `#9B1B49`）。这正是参考样本的做法：框线鲜艳、字是同色深版。③ 注记 `#857F72`→`#6B6455`（5.3:1）、解读句 `#6C7FD8`→`#4A5BC4`（5.2:1）、虚线 `#B3A98F`→`#8C8471`（3.3:1，图形元素达标）、`ink2` 也压深一档。**改后九项全部 ≥4.8:1**。④ **字号整体上调一档**（正常标签 12.5→13.5、注记 11→12、小字 10.5→11.5，结论 17→18），并记下一条能算的账：**viewBox 1080 的图进公众号，手机上缩放比约 0.32，图上 13.5px 只剩 4.4pt**——所以小字下限定为 11.5px，真读不动就该拆图或改竖版，不是继续调小。⑤ 规范里加了硬判据：**每个颜色上图前先算对比度，正文类 ≥4.5:1、图形 ≥3:1，别靠眼睛估**。

- **2026-09-03（五）**：**线条改单笔，去掉「涂描感」**，并给「要不要直接用 Excalidraw」定论。**升级模块**：配图页四张图的 rough.js 参数、`figs/*.png` 重出、`skills/diagram_style.md` §三（线条参数 + 新增「为什么不直接调用 Excalidraw」）。**触发原因**：用户反馈「方框边缘像涂描了很多次，还有笔画突出来了」，并问能不能直接调 Excalidraw 来画。**具体变更**：① 病根是 rough.js 的两个默认值——**`disableMultiStroke` 默认 false，即每条边画两遍**（涂描感），**`preserveVertices` 默认 false，线端被随机化**（拐角笔画戳出去）。两个都开，再把 `roughness 1.0 → 0.55`、`bowing 1.0 → 0.5`，线宽补一档（单笔比双笔看着细）；② **记下一条反直觉事实：Excalidraw 那种圆滑方框不是「更手绘」，是更不手绘**——Excalidraw 内部就是 rough.js，它的 sloppiness 滑块就是 roughness，参考样本里的干净圆角框是 Architect 档（≈0），我们原来跑在 Artist 档；③ **Excalidraw 判为不引入**，四条理由写进 skill：内部同为 rough.js（拿到的是同一个渲染器加重壳）、**手写体没有汉字**（英文样本看不出，中文图会掉字体）、GUI 应用程序化生成要 npm + 构建（现在是零依赖单文件）、它唯一的真优势是「产物能用鼠标改」——那条需求的正解是把我们导出的 SVG 拖进 Excalidraw / Figma 改，而不是搬管线。

- **2026-09-03（四）**：**FIG 2 结构性画错——单 die 画成了整颗 GPU**，已改为双 die + NV-HBI。**升级模块**：配图页 FIG 2、`figs/f2.png`、`skills/diagram_style.md`（§五 补一条）。**触发原因**：核对图注时发现 `图文精读` 里图 2 的图注写着「封装基板中央是**两颗**由 NV-HBI 相连的 logic die，两侧共八堆 HBM3E」，而那不是主模型写进去的那句（原句是「中央是 logic die，两侧是 HBM 堆叠」）；不论出自谁手，**这句是对的，且它抓到了图上的结构错误**——Blackwell / Blackwell Ultra 的一颗 GPU 是**两颗 reticle 尺寸的 die 经 NV-HBI 相连、对外呈现为单个 CUDA device**，图上却画成了一颗 die。**具体变更**：① FIG 2 改画两颗 die，中间加 NV-HBI 双向连接与标注，SM 阵列拆成两组（每组 3×5），每颗 die 各画一条 L2 条、数字统一标为「L2 cache 合计 126 MB」（官方 126 MB 是整颗 GPU 的口径，**不按 die 拆成 63 MB**，那是推算不是文档）；② HBM 明确标成左右各 4 堆共 8 堆；③ 图下补一行「两颗 die 经 NV-HBI 相连，对外是一颗 GPU」，`aria-label` 同步重写；④ 正文四处图片的 alt 与新图题对齐（alt 是图挂掉时的唯一线索，且 `notion_upload.py` 降级占位就取 alt）。**教训**：这是规范 §五「器件按其真实构造画」第二次被触发——上一次是晶圆级方案画成普通切割晶圆，这次是 die 数。**画硬件之前先查封装拓扑，不要凭"一颗芯片一个方块"的直觉画。**

- **2026-09-03（三）**：**图内文字改版——标题加重、靛蓝紫贯穿、语域分三档**。**升级模块**：`skills/diagram_style.md`（§三 颜色表与版面骨架、§四 新增「标签的语域分三档」）、配图页与 `figs/*.png` 全部重出。**触发原因**：用户反馈「图上有些颜色不够突出，比如标题」「底部图解可以用深蓝紫」「内容的语域应该更专业」。**具体变更**：① **诊断纠偏**——标题不突出**不是对比度问题**（`#2C3E7B` 在暖纸上有 8.8:1），是字重与饱和度问题：霞鹜文楷没有粗体，标题只能靠字号和颜色撑。修法两条：换成更饱和的靛蓝紫 `#3B4FB8`，并用 `stroke` 同色 + `stroke-width:0.7` + `paint-order:stroke` 模拟半粗；② **靛蓝紫贯穿顶底**，结论句 `#3B4FB8`、解读句 `#6C7FD8`，并写明**这支颜色只用于文字、从不画框**，因此不与「内存受限」的 `#1971C2` 冲突；字号 图题 22→26、结论 16→17、副标题与解读 12.5→13，顶带 100→104、底带 118→122；③ **语域分三档写进规范**——术语位用行业标准写法（`QKV 投影` 而非「算出 Q/K/V」、`算术强度低` 而非「搬得多算得少」、`Tensor Core 等待访存` 而非「在等数据」）、说明位平实但**动词全套统一**（访存一律「读取/写回/传输」，废掉满图的「搬」）、结论位写成可引用的判断句并**删掉俏皮话**（「算力吃得饱」「用不着拼车」）；④ **明确一条例外：嘉宾的比喻照留**（公交车/私家车有出处，属原片资产，不因「要专业」而删）；⑤ **明确一条判据：专业 ≠ 书面化**——「对 KV cache 做全量读取」不比「读取整段 KV cache」专业，只是更绕；⑥ 引入 `算术强度`（arithmetic intensity）这个正确术语上图，并在正文对应段补了一句括注，避免图上出现正文没交代过的词。

- **2026-09-03（二）**：**配图风格 v2——暖纸底、深描边浅填充、固定四段式版面**，并给出两个外部图表 skill 的取舍。**升级模块**：`skills/diagram_style.md`（§三 线条／颜色／新增「版面骨架」、新增 §六「要画数据图表时」、§八 自检补两条、§九 外部 skill 补两行）、`render_figs.py`（导出底色改读 `--paper`）、`output/Neil Movva - Making AI 10x Cheaper/图解 - 一个 Token 的芯片之旅.html` 与 `figs/*.png` 全部重画。**触发原因**：用户给了推特博主 Akshay 的六张配图作参照，要求借鉴其「色彩鲜艳、久读不累」的做法，并点名三条——黄纸底色、虚线／实线做板块划分、规范每张图的标题与图下解读；同时问 GitHub 的 lieflat-charts 能不能用来画数据图表。**具体变更**：① **底色从近白 `#F7F6F2` 换成暖黄纸 `#F7F2E4`**，页面去掉点阵网格与卡片描边，图直接落在纸上；② **填充从 hachure 全面改成实心浅 tint**，每个语义色变成「深描边 + 浅填充」一对（计算受限 `#E8590C`/`#FFE3C7`，内存受限 `#1971C2`/`#D3E9FA`），这是「鲜艳但不刺眼」的真正来源，也顺带根治了上一版「文字压在斜线网底上读不清」的毛病；③ **方框全部改圆角**（`rc.rectangle()` 无圆角，改用圆角路径喂 `rc.path()`），**虚线＝逻辑分组／边界、实线＝具体部件**写成硬约定，新增区域徽章 pill（`片内` / `片外`）；④ **版面骨架固定为四段**：顶带 100px 放图题（22，`#2C3E7B`）＋副标题，底带 118px 放一条虚线＋结论句（16）＋解读句（12.5）；**图题必须画进图里**——导出的 PNG 要能单独站住，公众号里一张图就是一屏，读者看不到网页上的 `<h2>`；实现上图身画在 `translate(0,100)` 的 `<g>` 里，原坐标一律不用改；⑤ **`render_figs.py` 修了一个真 bug**：导出底色读的是已被删掉的 `--card`，落空后退回白底，四张 PNG 全是白底、黄纸只在网页上有；改成读 `--paper` 并回退到 `body` 背景色；⑥ **新增 §六 图表判据**（先选类型再画／一张图一套色系／图元映射真实值／示意图必须注明），要画 chart 就用 rough.js 在同一套系统里画，不引第二套图表引擎；⑦ **lieflat-charts 判为「借判据、不安装」**，三个原因：许可是 **PolyForm Noncommercial 1.0.0**（公众号沾商业属性即需另行授权，发布物上的风险不该藏着）、Inter ＋低饱和编辑风与手绘暖纸是两个系统、**Chart.js 渲染到 canvas 而 `render_figs.py` 只序列化 `<svg>`，导不出来**。

- **2026-09-03（一）**：**配图口径改为「图上不放出处、数字必须核到官方」，并补上导出管线**。**升级模块**：`skills/diagram_style.md`（新增 §五 数字与专名的核对、§六 导出与嵌入正文，重写 §七 自检清单，§八 外部 skill 加 frontend-slides 一行）、新增 `render_figs.py`、`assets/fonts/subset_font.py`（`ALWAYS` 补标点）。**触发原因**：用户看过《一个 Token 的芯片之旅》四张图后立了两条规矩——① 时间戳与出处不进图，图要干净、只讲逻辑，口径集中到文末出处区；② 图上的技术词与数字必须先核对再画。**具体变更**：① **出处不进图**——原规范写的是「数字必须标出处（原片时间戳或厂商资料）」，与新口径直接冲突，已改写；图内只保留非时间戳的归属说明（`嘉宾说的 happy path`、`比喻出自原片`），因为「这是谁说的」属于图要表达的内容。② **厂商规格以官方文档为准，嘉宾口头约数不上图**——本次核对当场抓到三处偏差：嘉宾说 HBM「约 10 TB/s」，NVIDIA B300 官方是 **8 TB/s**；嘉宾说逻辑芯片「约 500 MB SRAM」，官方可查的是 **L2 126 MB**（Blackwell Tuning Guide），每 SM 寄存器 **256 KB**、shared memory 上限 **228 KB**；量级对得上但具体值不对，直接照抄就是错的。图上换成官方值，嘉宾口径降级为出处表里的一行。③ **没有公开口径的量不许编**——NVIDIA 不公布片上 SRAM 聚合带宽，图上只标容量、带宽用定性表述。④ **新增 `render_figs.py`**：起临时本地服务器把配图页喂给浏览器，页面把每个 `<svg>` 连同内联 `@font-face` 一起序列化，再用 canvas 按 `viewBox` 精确栅格化成 PNG 回传落盘；**默认只出 PNG**（`--svg` 才额外出矢量），成品目录 `figs/` 只放最终要进文章的图。**刻意不引 Playwright / headless Chrome**——多 150 MB 依赖，且截图带浏览器缩放误差，精度不如按 viewBox 栅格化。⑤ **`subset_font.py` 的 `ALWAYS` 补上 `「」〔〕＝`**：这几个正是配图规范自己在用的标点，原来不在保留集里，裁完会掉成豆腐块。⑥ **自检清单补三条硬项**（借 frontend-slides）：零截断、零重叠、页面不横向滚动；并写入本次教训——**带文字的方框不要填 hachure**，文字压在斜线网底上读不清。⑦ **`notion_upload.py` 接上 Notion File Upload API**：正文里 `![alt](figs/f1.png)` 这类本地相对路径不再降级成占位文字，而是三步传上去（`POST /v1/file_uploads` → `POST {upload_url}` 发 multipart 字节 → 块里用 `{"type":"file_upload"}` 引用），图由 Notion 托管、不需要图床。三个坑记下来：**（a）版本号**——2022-06-28 那版根本没有 file_upload，但全局升版会打断查重（2025-09-03 起 database 改成 data source 语义），所以只在建 upload、发字节、以及**含 file_upload 块的那一批** children 上用新版本号；**（b）解析要保持纯函数**——上传放在 `resolve_local_images()` 里做，`parse_markdown()` 只留内部占位块，测试才不用起网络；**（c）重试会传 0 字节**——`files=` 传文件句柄的话，第一次发完句柄就到 EOF，`http_utils` 的退避重试会把空内容发出去，改成先读进内存。另加兜底：处理完不许有内部占位块残留（残留会让整批 95 个块一起 400，而报错只说 validation error，很难定位到是哪张图）。⑧ **Exa MCP 定为查证首选**并写进 skill——同一批数字，通用搜索只纠出「10 TB/s 应为 8 TB/s」，换 Exa 一次就翻出**封装是双 die、由 NV-HBI 以 10 TB/s 相连**这个结构性事实，图上原来画成一颗 die 是错的，已重画。⑨ **frontend-slides 明确「没借」的部分**也写进表里：12 套风格预设与「每次三套风格让用户挑」（我们刻意锁死一种手绘风格保证跨图一致）、「只用 Fontshare / Google Fonts、禁自带字体」（中文做不到）、Playwright 截图导出。

- **2026-09-02（四）**：新增 **`skills/diagram_style.md` 配图规范**，并把手绘图字体落成项目资产。**升级模块**：新增 `skills/diagram_style.md`、`assets/fonts/`（字体 + `subset_font.py` + README）、`CLAUDE.md` Step 2 挂载、`.gitignore`。**触发原因**：用户要求给「公交车和私家车」「内存这道死结」两节配 SemiAnalysis 式工程图，随后明确改为要 Excalidraw 手绘风，并要求把可借鉴的外部 skill 写进工作流、字体统一。**具体变更**：① **风格定为手绘**——`rough.js 4.6.6` 走 jsdelivr（**cdnjs 上只有过时的 3.1.0**；Artifact 的 CSP 只放行 jsdelivr 的脚本），`roughness 1.0–1.2` + `hachure` 填充，**必须固定 `seed`** 否则每次渲染换一张脸；② **字体定为方案 A**——拉丁用 Architects Daughter、中文用霞鹜文楷 Lite，**技术名词一律保留英文原样**（MLP / Self-Attention / Weights / KV cache 等）；字体**必须子集化后内联**，因为 Artifact 的 CSP 只允许 fonts.gstatic.com 提供字体文件，且不内联就得赌读者本机有中文字体；新增 `assets/fonts/subset_font.py`，收全一篇成品的用字约 200–300 KB；13 MB 全量 TTF 不进 Git，README 里留了重下命令；③ **两种语义色固定**（计算受限 `#E8590C` / 内存受限 `#1971C2`），跨图一致、不许现编，默认不用透明度做层次；④ 从三个外部 skill 借判据但都不安装——coleam00（⭐4.7k）的「先判深度 / 多层缩放 / **渲染后必须自看** / 颜色单一来源」、Agents365-ai 的 **CJK 标签宽度翻倍**（西文 ×9、中文 ×18，否则截字）、ian-xiaohei 的「画认知动作」与**分镜表工作流**；并记下一个反直觉发现：**这些 skill 默认 `roughness: 0`，手绘要专门指定**；⑤ 写入本项目三次实际教训：晶圆曾被画成普通切割晶圆（与 Cerebras「整片晶圆刻成一块芯片」正相反，用户当场指出）、脚本改写把自闭合标签弄坏、SVG 表现属性里的 `var()` 在部分浏览器不生效；⑥ 规定图的文字规范——标题写「这张图在解释什么」而非对应章节、每图一句图释、去绝对化用词、比喻用画的、数字必须标出处。

- **2026-09-02（三）**：**`notion_upload.py` 新增 Markdown 表格支持**。**升级模块**：`notion_upload.py`（新增 `table_block()` + `parse_markdown()` 表格分支）、`tests/test_markdown_blocks.py`。**触发原因**：用户要求把 Neil Movva 那期的 Cerebras `📎 延伸` 改成芯片对比表；排查发现脚本从来没有表格支持——`|` 只是被当作「不并进段落」的行首字符，每一行会退化成一个带竖线的段落块，到 Notion 里就是一堆竖线文本。**具体变更**：① 新增 `table_block(rows, has_column_header=True)`，产出 Notion 原生 `table` 块 + `table_row` 子块；**每行单元格数补齐到 `table_width`（缺列补空、多列截断），否则 API 会整块拒收**；② `parse_markdown()` 在围栏代码块判断之前识别连续 `|` 行，用 `re.fullmatch(r"[\s:\-|]+")` 丢弃 `|---|---|` 分隔行；③ 测试从断言旧行为（「表格行各自成段」）改为断言新行为，新增 5 项检查：不退化成段落、恰好一个 table 块、首行识别为表头、单元格数补齐、分隔行被丢弃且表头与数据行都保留。**回归测试**：`python3 tests/run_all.py` 全部通过（6 组）。

- **2026-09-02（二）**：经用户批准（Step 6），**从 GitHub 上两个 MIT 许可的开源「去中文 AI 味」skill 里提炼可借鉴条款，写进本项目自己的文档**——只借模式判据，不装 skill、不引入外部依赖。来源：[MrGeDiao/shuorenhua](https://github.com/MrGeDiao/shuorenhua)（1.3k ⭐，25 条句式库 + 严重度分级 + protected-spans）与 [op7418/Humanizer-zh](https://github.com/op7418/Humanizer-zh)（16.4k ⭐，译自维基百科「AI 写作特征」，24 类模式 + 五维评分）。**升级模块**：`skills/publication_format.md`「三、去 AI 味」、`CLAUDE.md` Step 2.5、`check_transcript_edit.py`、`skills/reader_facing_review.md`。
  **筛选依据**：先把两个仓库的全部条目逐条对过我们已有的七条，约一半（二元对比、否定式列举、首先其次最后、总结式收尾、破折号、三段式、填充短语）已覆盖；再把**我们没有的**模式做成扫描器实扫两篇成品验证价值——结果几乎扫不出东西（动词名词化 1 处、其余全 0），说明现有规则已覆盖同一片地，**所以价值不在"跑现有稿子"而在"补前置判据"**。
  **具体变更**：① 新增「**先保护，再改写**」前置条款（借 protected-spans），把保护区分成**按字面保留**（数值 / 专名 / 标识符 / 引用原话，一字不改、不四舍五入）与**按含义保留**（状态、条件、比较关系、因果方向、嘉宾让步语，语义强度不得变）两级——这是最该借的一条，因为历史上最高发的硬伤全出在润色 / 压缩这一步，而去 AI 味本身就是改写动作；② 「禁用包装词」由一刀切黑名单改为 **Tier 1/2/3 分级**（Tier 2 只在同段聚集时才算，短段 2+、长段 3+ 才标记），解决误杀「然而 / 正是 / 无疑」这类单用合理词的问题；③ 新增五条模式：**装饰性细节**（红线，判定要"无来源 + 删掉后文不变"两条同时成立，补上了原红线只管"判断"、不管"细节"的缺口）、**动词名词化**（进行 / 实现 / 起到 + 抽象名词，研报腔的主要成因）、**虚假主语**（赋能 / 助力 / 驱动，但技术描述的非人主语除外）、**同义词躲避**（与既有的"术语统一"不是一回事，那条对齐 glossary、这条抓为避免重复而故意换词）、**借喻场混用**；④ 新增「**两条反向条款**」明确挡住外部工具与本工作流冲突的地方——**不要"注入灵魂"**（加第一人称 / 观点 / 跑题，违反正文不得加入素材外判断）、**不要清"过度限定"**（嘉宾的"我不确定 / 大概 / Probably"必须保留，那是忠实不是啰嗦）；⑤ `check_transcript_edit.py` 新增 `sentence_rhythm()`，打印句长变异系数与"连续三句长度相近"处数，**对无时间戳的图文精读也跑**（此前该形态直接 exit 0，等于没有任何机检），变异系数 <0.40 提示偏平，本项目已定稿两篇的基线是 0.50 / 0.58；⑥ `CLAUDE.md` Step 2.5 新增第 4 项「保护区回核」与第 5 项「装饰性细节排查」，原 4–7 顺延为 6–9；⑦ `reader_facing_review.md` 的评审 Agent 补一组**可选语感五维**（直接性 / 节奏 / 信任度 / 真实性 / 精炼度），只测语感、不替代忠实度维度。
  **回归测试**：`python3 tests/run_all.py` 全部通过（6 组）。

- **2026-09-02**：经用户要求，把**「元叙述＋冒号」句式**列为去 AI 味的禁用项。**升级模块**：`skills/publication_format.md`「三、去 AI 味」。**触发原因**：用户指出「‘XX：XX’这种结构，‘他由此给出一个具体的设计取向：’‘他把这个词拆得很具体：’‘他把话留了一半：’等等，很明显就是 AI 写的」，要求两种形态都尽量少用、更精炼。**具体变更**：① 新增禁用条款并给出**判定方法**——把冒号前那半句整个删掉、句意不变即为废话；② 给出三种改法（删引导语 / 冒号改逗号句号 / 引导语改写成陈述而非宣告）；③ 划出**三类合法冒号**（引出真问句、引出清单枚举、引出逐字引语）以免误删；④ 明确**只约束编者旁白**，对话形态里说话人自己嘴里的这种句式要保留，那是口语节奏；⑤ 附带新增「同一种编者注不要重复超过两次」。**同期落地**：该期图文精读从 59 处清到 6 处（剩下 6 处全部是引出问句或清单的合法用法），中文字 9986 → 9782；逐字稿编者旁白清 2 处（其余 20 余处都在对话行内，按新规保留）。注：其中「他把话留了一半：」正是主模型在上一轮语域返工时自己新写进去的，说明这条句式在无明文禁止时会被反复引入。

- **2026-09-01（二）**：经用户要求，把**嘉宾背景的写法规则**补齐三条。**升级模块**：`skills/publication_format.md`「嘉宾背景」段落。**具体变更**：① 新增强制条款「**这一块不受字幕约束**」——嘉宾背景不是转写的一部分，正文那条「不加入视频外内容」的红线不适用于本区块，把两者混为一谈会写出干巴巴的背景；② 明确**核实渠道与可信度排序**（本人 LinkedIn / 个人主页 / 公司官网 → 权威媒体报道与融资通稿 → 投资方 portfolio 页），要求至少交叉两个来源并在文末注明出处与日期；③ 新增强制条款「**要往『他凭什么牛』的那一面写**」——同一段履历选最能让读者服气的切面，用具体数字和有画面的动作代替头衔与形容词。**触发原因**：Neil Movva 那期的双 Agent 核验指出，嘉宾背景六项硬事实字幕里一条都没有、却只有一句集体括注，且写法是流水履历、缺「提前喊过」的记录；用户据此要求"嘉宾背景不需要跟逐字稿相关，领英等公开平台核实一下，主要是嘉宾身份最好往牛逼的点去写"。**同期落地**：该期两稿的嘉宾背景据此重写（十亿部 iPhone、"苹果的野心到 animoji 就到顶了"、2025 年 9 月公开 KV cache 计算器→三个月后创办 Sail 押的正是这件事、8000 万美元/4.5 亿估值/A 轮 Kleiner Perkins 领投+种子轮 Sequoia 领投、每周处理数万亿 Token），来源为 Fortune 2026-06-25 报道与本人个人主页。

- **2026-08-31（三）**：经用户确认（Ratchet Check），把**核心观点的三项回验**升级为**覆盖整个压缩层**（核心观点 / 章节标题 / 导语），并写明"怎么验"。**升级模块**：`skills/publication_format.md`（「核心观点的三项回验」段落）+ `skills/dialogue_transcript.md`（「章节标题」段落）。**触发原因**：Tara Seshan 那期逐字稿的 Agent Council 清出 4 处必改硬伤，**全部落在压缩层、正文本体逐段核完零硬伤**——`don't simply…but`（递进）被写成"不是…而是"（排他）、导语"几乎没人愿意公开说"被本篇正文当场证伪（嘉宾原话 `I'm sure many people have talked about this`）、`equally wrong` 加码成"一模一样致命"、以及章节标题把 `expanding your thinking of what's possible` 写成"野心"从而**说反了嘉宾的论点**（她认为野心恰恰是能抬的、想象力才是瓶颈）。这四处的字幕原句正文全部译对了。这与 2026-08-13 那次（演讲实录 4 处必改全在导读层）是**同型复发**，说明原规则的适用范围划窄了。**具体变更**：① 新增「怎么验」条款——回验依据必须是**字幕原句**、不得只据自己刚写的中文正文提炼，否则压缩误差在同一条链路上传递两次、失去参照物；② 适用范围从「核心观点」扩到「核心观点 + 每一个章节标题 + 导语」，理由是三者共同特征都是"把一大段压成一句"、且读起来通顺自查发现不了；③ 补第二条实例教训；④ `dialogue_transcript.md` 的章节标题条款增加"定稿前回字幕核"、"标题必须由字幕支撑而非由中文正文支撑"、以及两项重点查（论点说反 / 被同篇正文证伪）；⑤ 同处把节数口径从"一篇约 8 节"这个绝对数字改为**按每节字数定**（1000–1500 字为宜、超 1700 字视为多半扛了两个主题），并明确"一节只扛一个主题"与天然拆点的识别方法（嘉宾自己点出的"另外还有一个教训"、主持人另起一问）。

- **2026-08-31（二）**：经用户要求，把**播客雷达的榜单改为不落盘、只出 Artifact 预览**。**升级模块**：`CLAUDE.md`「工作流：播客雷达」。具体变更：① 触发命令去掉 `--md`，改为 `python3 radar.py run --days 7`；② 新增强制条款「榜单不落盘」——`--md` 是可选落盘开关，除非用户明确说存，否则一律不传，榜单走 stdout 后做成 Artifact 交付；③ 明确 **`radar_data/radar.db` 必须保留**，热度分靠它算频道基线，删库等于清空雷达记忆、热度失去可比性。**同步清理**：删除 `radar_data/reports/` 全部 5 份历史榜单（08-11、08-12 的 7d/14d/30d、08-31），目录体积 2.1M → 1.5M；历史榜单无保留价值，同一窗口随时可用 `radar.py report --days N` 从库里重出。**触发原因**：用户指出「只有我明确让你放进去的你才放，否则文件夹里的东西越攒越多」「最新那份也不用存，直接给我能预览打开的就行」，要求项目目录保持轻量。
- **2026-08-13**：经用户确认（Ratchet Check），为**核心观点**新增写完即做的三项回验。**升级模块**：`skills/publication_format.md`（「三块的篇幅上限」之后新增「核心观点的三项回验（强制）」）。具体变更：规定核心观点每条写完后**须回正文与字幕各验一次**——① **限定语有没有被压掉**（嘉宾说「我不确定」「很难说」「这取决于」，压缩后不得变成断言；把嘉宾拒绝下的判断替他下了等同硬伤，与「删减红线」第 4 条同类，只是发生在压缩环节而非删减环节）；② **数量级与单位有没有跟正文对齐**（正文译对、压缩时挂错量级是高发且最难自查的一类，因为读起来完全通顺；凡出现数字一律回字幕原句核对，不得凭正文印象改写）；③ **「迹象 / 早期信号 / 有希望」有没有被写成既成事实**（`signs of` / `early` / `promising` 是研究者对自己结果的克制表述，抹掉即拔高）。附实例教训：Chelsea Finn 那篇 Council 清出的 4 处必改硬伤**全部落在导读层、正文本体零硬伤**——`far more useful when...`（程度比较）被写成"只有…才真正有用"（排他条件）、`I'm not sure we'll have...` 被写成"不会有"、`strong signs of compositional generalization` 被写成"组合泛化出现了"、"一百万条轨迹对应 700 个机器人日"被错挂成"几百万"（差 3–10 倍），四处正文都是对的、错全出在压缩那一步。触发原因：本轮 Step 2.6 的必改项 100% 集中在压缩环节，且与 07-21 Elizabeth Stone（核心观点只覆盖到 50 分钟）、08-12 Ryan Greenblatt（立场拔高两处）属同一类反复出现的失败模式，此前只有针对"删减"的红线、没有针对"压缩"的回验。**测试**：本轮未改动任何 `.py`，未跑 `tests/run_all.py`。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的；雷达成熟度曲线待用真实数据重拟。
- **2026-08-05（六）**：经用户批注，为**开头三块**（导语 / 嘉宾背景 / 核心观点）补篇幅上限与「清废话」条款，并修正零重叠自检的误报口径。**升级模块**：`skills/publication_format.md`。具体变更：① **新增「三块的篇幅上限（强制）」**——嘉宾背景**最多一段、两三句话**（只留最硬的战绩与「提前喊过」的记录，其余代表作与履历细节一律砍）；导语一段、**不超过 5 句**；核心观点每条「一句加粗判断句 + 一到两句展开」，**展开不得复述判断句**。理由写明：这三块是读者最先看到的内容，必须准确且不能啰嗦。② **新增「清空转折废话」条款**——点名禁用「这句话不是修辞，而是……」「真正的分量不在 A，而在 B」「这不是 X，是 Y」这类句式，判据是"删掉之后意思不变就是废话"。③ **修正零重叠自检的口径**——原写"把专有名词（含数字、年份）各列一份、不应有交集"，实测会在裸年份上误报（导语的「2026 年上半年」与嘉宾背景的「2026 年 2 月出版」年份相同但事实不同）；改为比**同一个事实**而非同一个数字，并补出典型真重叠的形态与正确分工：**导语只下判断不带证据、嘉宾背景供全证据**。触发原因：用户指出 Bill Gurley 那篇的导语废话多、嘉宾背景过长；据此把该篇导语由 402 字压到 257 字、嘉宾背景由 577 字压到 300 字（两段三句）、核心观点六条逐条去冗；压缩时自检发现「提前喊过」的记录在导语与嘉宾背景各写了一遍，按新分工把导语里的年份与作品名删去、只留判断。同轮顺带补上核心观点的一处覆盖漏洞——拆节后新增的「循环收入」一节原本无任何核心观点覆盖，已并入第四条。遗留：导语所引 2026 上半年行业数据仍待回一手复核；Notion 表格块仍未支持；观察稿归档 / 上传路径仍是断的。
- **2026-08-05（五）**：经用户确认，为**嘉宾背景 / 导语 / 生成顺序**立三条跨形态规则，写入 `skills/publication_format.md`。**升级模块**：`skills/publication_format.md`、`skills/dialogue_transcript.md`、`skills/illustrated_deepdive.md`、`CLAUDE.md`。具体变更：① **新增「生成顺序（强制）」**——**正文 → 核心观点 → 导语 + 嘉宾背景 → 标题与钩子**，且**不只适用于精修，初稿同样按此**。理由写明：核心观点提炼自正文、导语又要基于正文与核心观点，倒着做必然返工，还会出现"核心观点覆盖不到后半场""导语承诺了正文没兑现的东西"这类错。据此**修掉三处旧顺序冲突**：两个 skill 的「面向发布的多轮精修流程」与 CLAUDE.md Step 4.5 原写的是"内容与价值 → 嘉宾背景 → 核心观点"，核心观点排在导语之后，与新规则矛盾，统一改为"核心观点 → 导语 → 嘉宾背景 → 写在最后"。② **新增「嘉宾背景」写法规范**——职责是让读者觉得"这个人值得听"，**必须先 WebSearch 补料、不能只用字幕里那点自述**（嘉宾谈自己往往轻描淡写，光靠字幕写出来平淡无味）；优先找三类硬料：可量化的战绩（数字比形容词有力）、有戏剧性的关键动作（主导过什么 / 推翻过谁 / 赌对过什么）、**"提前喊过"的记录**（此前在哪次拐点前做过判断、后来兑现没有——最能建立可信度、也最容易漏）；所有事实须可核实，**不得为张力编造或夸大**，拿不准标 `⚠️建议人工核对`。③ **新增「导语」写法规范**——说清"这篇编译为什么值得期待与关注"，**必须结合嘉宾的独特位置 + 当前行业趋势 / 时间点**（用可核实的当下数据把"为什么是现在读"钉住，行业数据同样要 WebSearch 核并交代口径），末尾落回开篇金句。④ **新增「嘉宾背景与导语：零重叠」铁律 + 分工表 + 可执行自检**——两块最容易撞车，规定"他是谁、凭什么"归嘉宾背景、"这篇为什么现在值得读"归导语，履历只在嘉宾背景出现；自检方法是把两段的专有名词（公司名 / 数字 / 年份 / 代表作）各列一份，**两份清单不应有交集**。触发原因：用户就 Bill Gurley 那篇的三个区块给了截图批注，指出嘉宾背景与导语重叠（两处都在写"二十五年风投、Uber/Zillow/OpenTable"）、且嘉宾背景写得没有张力、导语没有交代行业当下。据此重写该篇两块并 WebSearch 补料（Benchmark 2011 年 1200 万美元投 Uber、一度值约 80 亿且出自 4.25 亿美元的基金、IPO 个人回报逾 6 亿；2017 年主导罢免 Kalanick、Benchmark 当年 8 月起诉；2015 年 SXSW 预言"死掉的独角兽"、2016 年 4 月《On the Road to Recap》指烧钱率或为 1999 年的五到十倍；导语引 2026 上半年美国 86% 风投美元流向 AI、OpenAI 与 Anthropic 占全球逾四成、非 AI 融资经通胀调整低于 2020 年一季度），重叠自检确认两段专名清单交集为空。遗留：导语所引 2026 上半年行业数据来自二手聚合源，发布前需回一手（PitchBook / Crunchbase）复核，已记入该篇交接文档待办；Notion 表格块仍未支持；观察稿（模式 C）归档 / 上传路径仍是断的。
- **2026-08-12**：经用户确认（Ratchet Check），补上 **Council 核实 Agent 死亡时的降级路径**与**雷达 enrich 的抓取强度上限**。**升级模块**：`skills/reader_facing_review.md`（「Agent Council 终审协议」新增一节）、`radar.py`（enrich 默认参数与文档字符串）、`CLAUDE.md`（雷达节「已知边界」）。具体变更：① **核实 Agent 中途死亡的降级路径**——此前铁律只写"必须是全新 Agent"，未覆盖"核实 Agent 跑到一半挂掉"这一情形（已两次撞上：2026-06-28 session 限额、2026-08-12 API 额度耗尽），两次都由主模型临时代行、事后才发现无章可循。现明确：主模型**可以**代行以便当轮清掉硬伤，但必须同时做到三件事——只依据原始字幕逐条判定（代行的是核实、不是自辩）；在 `workflow_execution.md` 与该稿 `交接文档.md` **双处标注为降级路径**；**定稿终审时该环节必须由全新 Agent 重做一遍**，且交接文档「⏳待办」保留该条直到补完。附理由：主模型核实自己刚写的稿本身就违反第一条铁律，它对自己的错归、拔高、译反最不敏感，而这三类恰是历史最高发硬伤，降级只是止损、不是等价替代。② **enrich 抓取强度下调并写死上限**——`--enrich-limit` 默认 150 → **80**，每条随机停顿 1.2~2.8 秒 → **3.0~5.0 秒**，`enrich` 子命令的 `--limit` 默认 0（不限）→ 80。触发实测：2026-08-11 用 4 线程连抓 300 个 watch 页后，YouTube 对本机 IP 软限流，**且限流延续到次日**——第二天 RSS 间歇返回假 404，补跑 6 轮 poll 仍只有 7/20 频道拿到当日采样，榜单被迫沿用前一日数据。该现象此前只在代码注释里记了"当天会被降级成空壳页"，没记"会跨天"，也没在 CLAUDE.md 里对使用者提示，故一并补进雷达节「已知边界」。**测试**：`python3 tests/run_all.py` **6 组全部通过**。触发原因：Ryan Greenblatt 那篇的 Step 2.6 与 Step 5.5。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的；雷达成熟度曲线待 3 周后用真实数据重拟。
- **2026-08-11**：经用户确认，新增**播客雷达（选题发现层）**——工作流从"用户给链接"向前延伸到"系统找候选"。**升级模块**：新增 `radar.py`、`radar_channels.json`、`tests/test_radar.py`；`CLAUDE.md`（新增「工作流：播客雷达」节）、`README.md`、`PLAYBOOK.md`、`.gitignore`（`radar_data/`）、`tests/run_all.py`。具体设计决策：① **数据源走频道 RSS 而非 YouTube Data API**——RSS（`/feeds/videos.xml?channel_id=`）自带 `media:statistics views`，最近 15 条上传全有播放量，零 API key、零配额、零成本；只有时长要抓 watch 页补（`enrich`，已内置"响应体 <50KB 判静默限流、连续 3 次提前收工、增量落盘"的礼貌抓取——实测 4 线程并发抓 300 页即触发 YouTube 静默限流，返回 200 + 空壳页而非 429）。② **排序用相对热度、不用绝对播放量**——`热度 = 本片播放量 ÷ 该频道自身基线`，基线首选成熟视频（≥21 天）播放中位数；高产频道 RSS 整页不满 21 天凑不出成熟样本时，**降级用成熟度曲线反推生命周期播放量**（首跑即撞上：YC / 20VC / AI Engineer 全部无基线、从榜单消失，降级后修复）；被打分视频**必须排除出自家基线**，防小样本频道爆款自证。成熟度曲线现为拍脑袋先验，`samples` 表攒够 3 周后应改为按真实数据拟合（TODO 已写在代码里）。③ **按需手动触发、明确不挂定时**——经用户决策：脚本本身零 token，但出编辑筛选名单需要 Agent 解读，定时跑就是定时烧 token；改为 `CLAUDE.md` 口令工作流（说「跑雷达」→ `radar.py run --days N` → 三档编辑筛选：强烈建议 ≤3 条 / 观察名单 / 明确不做），与「开始转录」同一交互模式。代价也写明：RSS 只回 15 条，跑的间隔别超过窗口一半，否则高产频道漏片；窗口内跑 ≥2 次后自动切真实增速。④ **雷达输出是标题层筛选**，不抓字幕；用户选中后才进阶段 -1 正式预判，两层判断（火不火 / 值不值得做）正交。⑤ 新增 `tests/test_radar.py` 6 项，钉住"静默扭曲排名"类失败：成熟度曲线单调性、基线正常路径与降级路径、爆款不污染自家基线、RSS views 解析、真实增速需 ≥2 次采样。全套 6 组测试通过。触发原因：用户提出想要"YouTube 播客监控"产品并拍板自用最小版；首跑即产出 8/11 榜单并给出周选题名单（强烈建议 OpenRouter CEO×20VC / Flo Crivello×Cognitive Revolution / Garry Tan×YC）。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的；成熟度曲线待 3 周后用真实数据重拟。
- **2026-08-09**：经用户确认（Ratchet Check），为「批量命名规则」补上**删除授权分支**与**待用字幕的 summary 导读**。**升级模块**：`CLAUDE.md`（阶段 -1「批量命名规则」第 3 条）。具体变更：① **用户明确说删就直接删**——不必二次确认、不必论证代价，执行日志一句话带过即可；原规则"未选用的一律移入 `transcripts_pending/`、不删除"改为"**默认**不删除"，并写明该默认只约束 Agent 自己的判断，不用来驳回用户的处置决定。② **留存待用的字幕必须配 summary 导读**——与 JSON 同目录同名、后缀 `.summary.md`，内容固定四块：基本信息表（video_id / URL / 场合 / 时长与覆盖率 / 抓取日期 / 预判分 / 当前状态与暂缓理由）、这期在讲什么（一句话定性 + 形态）、**章节地图**（时间戳 → 内容、覆盖全片）、**Highlight**（带时间戳的金句与猛料）+ 开工前必须处理的核对项（已知字幕听写错误、待查证的外部事实）。目的是让日后接手的 Agent 不必重读全片字幕就能判断要不要做、从哪下手——此前 `transcripts_pending/` 里只有裸 JSON，除了文件名之外零信息，等于把预判阶段的全部判断丢掉、下次得重来一遍。本次已为 `Jeff Dean - The 1% Rule for Building in AI.json` 补出首份范本。触发原因：本轮四链接批量预判后用户判定全部不做、指令删除其余三份，并指出两点——删除类指令不必写得那么详细，以及保留的字幕应附摘要文档说明这期主要内容与 highlight 对应章节。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的。
- **2026-08-05（四）**：经用户确认，**把发布格式提炼为跨形态单一来源**，并补齐本轮遗漏的四条规则（A/C/D/E）。**升级模块**：新增 `skills/publication_format.md`、`skills/dialogue_transcript.md`、`skills/illustrated_deepdive.md`、`CLAUDE.md`。具体变更：① **新增 `skills/publication_format.md`（跨形态发布格式统一规范）**——用户明确"无论逐字稿还是图文精读都按这套"，故不往各 skill 各抄一份（本项目已因 `AGENTS.md` 漂移 65 行吃过亏），改为单一来源 + 各 skill 指针。内容含：共有壳区块与顺序（开篇金句/主图/署名行/导语/`# 嘉宾背景`/`# 核心观点`/形态主体/`# 写在最后`/`**本文来源**`）、开篇金句作为题眼须三次呼应、排版硬性（`#` 标题、禁 `---`、禁 YAML 与双链、媒体占位）、**空行规则**（双空行→Notion 空段落块；正文章节标题下留空块、非正文标题不留；说话人标签前留空块）、说话人 emoji 固定约定（主持人 🎙️ / 男嘉宾 👱‍♂️ / 女嘉宾 👩，不得凭姓名推断性别）、`>` 与 `💡` 分工、去 AI 味清单，以及四种形态如何接入的映射表。② **`illustrated_deepdive.md` 接入**：9 段式重排为"共有壳 + 模式 A 专有"，**原「内容与价值概括」改称「导语」、原「结尾收束段」改称「写在最后」并固定用 `# 写在最后` 标题**（双锚点关系不变，历史稿不回改）；摘要钩子、封面 Recraft Prompt、路线条、减负装置、系列回链标为模式 A 专有保留。③ **规则 A · 候选池核实前置闸**（写入 `dialogue_transcript.md` A/B 协议之前）——**不得直接采信用户或任何二手来源对外部源的描述**（谁说的 / 提出了什么框架 / 在哪一段），须回原始素材验证；验证不了就不进候选池，更不得顺着该前提往下推理。附实例教训：某篇三处错归的源头不在字幕，而在主模型接受了"某分析框架是嘉宾自创"这句二手描述——**链接核过、标题对得上，唯独没核这句描述**，结果该框架实属该期主持人。④ **规则 C · 用户推翻 Agent A 裁决时的补偿动作**——用户有权保留 A 判不放行的条目，但必须同时：文末「本文来源」把这些条目也逐项列全（正文不区分来源时这是唯一追溯入口）；交接文档记录 A 的原始判定与用户裁决（否则接手者会默认全部过闸）。⑤ **规则 D · 无时间戳成品的机检不算通过**（写入 `CLAUDE.md` Step 2.5 与 skill）——脚本对无时间戳成品输出"无需扫描"并 exit 0，那是**跳过不是放行**；对这类成品再做任何删减或结构调整（拆节/合并/调序）必须回 `transcript.json` 人工核校并在交接文档写明，**不得以 exit 0 声称已核校**；同时把"先带时间戳跑机检→全绿后再去时间戳"的时序铁律从 skill 同步进 CLAUDE.md。⑥ **规则 E · 拆节不得编造主持人提问**——新起一节可直接由嘉宾发言起头（范本「砍掉没用的流程：JIT 即时规划」即此写法），需要过渡时用 `💡` 提炼行，**不得伪造问句**；理由是"把话塞进主持人嘴里"与"把外部发言错归给嘉宾"是同一类错。⑦ **`CLAUDE.md` Step 2 两种模式均须先加载 `publication_format.md`**；**Step 4 补一句"每次重传都会归档旧页并重建、Notion 链接每次都变"**，要求交接文档同步现行链接、定稿前不对外分发。⑧ `illustrated_deepdive.md` 的「外部素材处理」补铁律：**把嘉宾在别的对谈里的发言并进本篇且不再区分来源时，模式 A 与模式 B 共用同一套「候选池核实 → A/B 双 Agent 并入协议」**，不得因"图文精读本来就是重构"而跳过。**测试**：`python3 tests/run_all.py` 5 组全部通过（本轮未改 `.py`，作回归确认）。触发原因：用户在本轮工作流复盘时主动问"还有哪些遗漏但必须写进规则的"，由主模型盘点本轮实际发生过、却未落成规则的行为，逐条提报后用户圈定 A/B/C/D/E 五条并明确 B 为跨形态通用。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的，且尚未接入 `publication_format.md` 的映射表之外的细则。
- **2026-08-05（三）**：经用户确认，为**外部素材并入嘉宾发言**建立 **A/B 双 Agent 并入协议**，并据此放开原先的绝对禁令。**升级模块**：`skills/dialogue_transcript.md`（「外部素材处理」整节）。具体变更：① 原规则写的是"统一当外部事实，**绝不**混入对话体冒充嘉宾原话"，现改为**默认仍不混入、但「嘉宾在别的对谈里就同一话题的发言」是唯一例外**，且该例外必须走协议、不得直接动手。② **新增「A/B 双 Agent 并入协议」**——**Agent A（核实，先跑）**逐条判定候选内容，**两道闸门必须同时通过**才放行：**归属闸**（确为嘉宾本人所述、不是同场主持人或其他嘉宾的话，判不出来一律不放行）与**相关闸**（与拟并入的那一节强相关，是对该节已有论点的机制 / 证据补充而非新开话题，并入后不改变论证走向、不打乱时间线、不需要为它另起铺垫；**只要需要"为了容纳它而调整结构"就不放行**）；A 输出带"位置｜内容｜归属判定与依据｜相关性判定与理由｜放行与否"的清单。**Agent B（并入，后跑）**只拿 A 放行的条目执行，改写成第一人称接进对应那一轮发言，**不自行增补、不自行改判**。**铁律：A 与 B 必须是两个不同的 Agent**，不得由同一个 Agent 既核实又并入——自己核实自己等于没核实（同 Step 2.6 Agent Council 的"模型不能给自己打分"）。③ **并入之后仍强制两件事**：文末「本文来源」保留**双来源合并声明**（写明补入了哪几处、这些内容为嘉宾本人所述但并非在本次访谈中说出、同场其他人的观点未被引入），正文不再区分之后这是全篇唯一的来源自证、**不得删除**；数字与预测类内容（金额、占比、增长预测，及"我目前不担心 X"这类当下判断）保留原始日期锚，机制类不受此限。④ 原「说话人归属铁律」保留不动，重新定位为 **Agent A 归属闸的判定方法**（称呼语、指向性表述、身份自陈、立场差、文体计量交叉验证）。⑤ 同步修掉三处受影响的旧表述：编辑原则的「指定容器」、引用块用途说明、文末「本文来源」的外部素材声明要求。触发原因：Bill Gurley 那篇用户拍板把 BG2 内容完全并入嘉宾发言、不做区分，与既有禁令正面冲突；该禁令源自上一轮 Ratchet Check（三处主持人发言被错归到嘉宾名下），不宜简单删除，遂由用户给出 A/B 双闸门协议替代——既放开了并入，又把"归属"和"相关性"两个风险各留一道闸。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的。
- **2026-08-05（二）**：经用户确认，把 Notion 范本《Claude Code 工程负责人 Fiona Fung：如何打造全世界最 AI Native 的工程团队?》定型为**对话体逐字稿的发布形态标准**，并配套改造上传脚本。**升级模块**：`skills/dialogue_transcript.md`、`notion_upload.py`、`tests/`（回归验证）。具体变更：① **「输出结构」整节重写**——由旧的 6 段式改为 8 段式（开篇金句 / 主图 / 署名行 / 导语 / `# 嘉宾背景` / `# 核心观点` / 正文各章节 / `# 写在最后` / 文末 `**本文来源**`）；章节标题一律 `#`；**全篇禁用 `---` 分割线**；核心观点由三级标题清单改为**无序列表**（加粗判断句 + 展开）；「访谈信息」元数据块从开头移到文末。开篇金句确立为**全文题眼**，须在导语末与「写在最后」各呼应一次（共三次）。② **新增「关于时间戳」**——发布形态不带 `[MM:SS]`，但 `check_transcript_edit.py` 的缺口扫描全靠时间戳，故钉死顺序：**带时间戳生成 → 机检全绿 → 才去掉时间戳**；去掉之后若还要删减，必须回 `transcript.json` 人工核校。③ **对话格式改写**——说话人标签为 `emoji + 空格 + 姓名`，**不加冒号**；**emoji 固定约定：主持人 `🎙️`、男性嘉宾 `👱‍♂️`、女性嘉宾 `👩`**，并写明性别须有可核实依据、不得凭姓名推断，无从判断时用中性 emoji。④ **新增「空行规则」并为此改造 `notion_upload.py`**——原解析器直接跳过所有空行，Markdown 的留白到 Notion 不留痕；现改为**连续 ≥2 个空行 → 落一个空段落块**（文档首尾不落，避免莫名空行），这是内容无关的通用规则，不把"说话人"这类语义写进上传器。据此规定：说话人标签前空两行、一级标题下空两行、标签与发言之间空一行、同一人不同语义段之间空一行（不空行会被合并成同一段落块，在 Notion 里粘成一坨）。⑤ **删除范围细化**——明确要删开场与结尾寒暄、边想边说的过程、纯附和回合、`[笑]` / `[掌声]` 舞台提示、广告口播；同时**新增反向约束「让步与限定语必须保留」**（"我说话会比较小心""我不确定你躲得开""这不是说它没有价值"），删掉等于把立场说硬，呼应「删减红线」第 4 条。⑥ **消除四处因本次改动产生的自相矛盾**：核心定位与演讲实录变体的导读层构成、精编压缩里已不存在的「以下为完整逐字稿」大节标题、编辑原则「不插入编辑评论」与模板的 `💡`/「写在最后」冲突（改为编者的话只能进三个**指定容器**）、以及一边说删 `[笑]` 一边说可标 `[笑]`。自检 Checklist B 同步扩为四条（含金句三次呼应、emoji 一致性、去时间戳的时序）。**测试**：改动 `notion_upload.py` 后跑 `python3 tests/run_all.py`，**5 组全部通过**；另用 `parse_markdown` 做了端到端验证，确认双空行确实落成 Notion 空段落块。触发原因：用户提供范本并要求据此改版，随后又就正文格式给了截图批注（去冒号、空行块位置、嘉宾 emoji）。实测同一篇稿子块数由 135 → 206 → 278，多出的正是分段与留白。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的。
- **2026-08-05**：经用户确认（Ratchet Check），为外部素材并入新增**「说话人归属铁律」**及配套两节。**升级模块**：`skills/dialogue_transcript.md`（「外部素材处理」段落）。具体变更：① 素材类型表新增一行「嘉宾在**别的多人对谈**里的发言（另一档播客 / 圆桌）」，明确可作「延伸」引用块补机制，但每条引用必须先定案说话人。② 新增**「说话人归属铁律（并入多人对谈类外部素材时强制）」**——指出自动字幕在这类素材上极不可靠（`>>` 常只标声学事件而不标换人，长段对谈可整段无标记）；规定无标记区段**必须用内证判定归属**（称呼语、指向性表述如"你 18 个月前提过 / in response / to your point"、身份自陈、立场差），必要时用**文体计量交叉验证**（统计口头禅在各人已确认轮次中的分布，再看争议段落在谁的分布里）；**拿不准的一律不引入**，并写明"不得因语气像 / 思路像 / 符合他一贯观点就默认属于嘉宾——**越像越危险**，因为读者同样分辨不出"；要求文首「逐字稿说明」的**兜底声明写实**（须写明引用范围，如"仅含 X 本人发言、该期主持人观点未引入"，笼统写"来自 X 的发言"会把个别错归放大成全篇错归）；引注一律指向被引句首而非该轮起点。附实例教训。③ 新增**「外部素材的时间戳写法」**——外部时间戳不得用 `[MM:SS]` 方括号，须写成 `（<来源缩写> MM:SS）`，因为 `check_transcript_edit.py` 会把方括号时间戳当主源时间轴解析、制造假逆序硬伤与幻影删除区间，且读者会读成同一条时间轴。④ 新增**「外部素材的时效标注」**——跨时间引用分两类：机制类（原理、会计路径、历史对照）不过期；**数字与预测类，以及"我目前不担心 X""我现在会买"这类当下判断都会过期**，必须显式标注原始日期、不得当成嘉宾此刻立场，图表尤其要标（图会被单独截出去传播）。触发原因：本轮 Bill Gurley（Forbes Under 30）逐字稿把 BG2 那期里**主持人 Brad Gerstner** 的三处发言写成了嘉宾的判断——交易连续谱与"若没有这笔投资这些产品还会被买走吗"的判定式、"只有一个买家的芯片"红旗、以及"作为英伟达股东我并不担心"（后者等于替嘉宾虚构了持仓身份）；三处全部由 Step 2.6 Agent Council 才捞回（评审 Agent 抓到两处、核实 Agent 用文体计量补出第三处），初稿自检毫无察觉。同轮机检还因 BG2 方括号时间戳误报 8 处逆序 + 一段 23:29 幻影删除区间。原 skill 的外部素材条款只规定了"不混入对话体冒充嘉宾原话"，默认外部素材是单一说话人，未覆盖多人对谈这一情形，遂补齐。
- **2026-08-03（三）**：经用户确认，第三轮优化——**把最后一条靠自觉的铁律代码化 + 减少权限打断**。**升级模块**：新增 `check_transcript_edit.py` 与 `tests/test_edit_check.py`、`.claude/settings.local.json`、`CLAUDE.md`、`PLAYBOOK.md`、`README.md`、`skills/dialogue_transcript.md`。具体变更：① **新增 `check_transcript_edit.py`（精编稿体检）**——把「删减红线：时间轴缺口扫描」从模型自觉变成脚本计算。做两件事：**时间戳单调递增校验**（出现逆序即 exit 1，属硬伤）与**≥2 分钟删除区间扫描**（含开场与片尾，自动在同目录找 `transcript.json`，逐段摘出原字幕）。额外做一层启发式：区间内出现让步 / 限定 / 自我收口语（of course、that said、当然、不过、也有例外…）的标 🔺 高风险——正对应 skill 第 4 条"删减改变立场强度等同硬伤"。脚本**只负责算和摆证据，不替编辑判断该不该删**：理由仍只能落在四类（广告口播 / 铺垫性长问 / 重复论证 / 纯附和回合），报告里每段留白待认领。支持 `--min-gap` 调阈值、`--json` 出结构化结果；成品无时间戳（图文精读等形态）直接放行不报错。接入点：`skills/dialogue_transcript.md` 缺口扫描第 1 条由"用相邻时间戳差值扫"改为"用脚本扫，不要靠眼睛"，CLAUDE.md Step 2.5 新增第 5 条"带时间戳的成品必须先跑机检、结果随本步汇报，不得跳过后直接说已核校"。配套 `tests/test_edit_check.py` 12 项（总计 5 组 50 项）。触发原因：该规则是 2026-07-21 因 Elizabeth Stone 那篇静默删掉 [50:22–52:54] 整段关键问答而立的，但只写成了 skill 里的自觉条款；日志显示同类失败反复出现，而覆盖率校验一旦下沉进脚本就再没犯过——同样的办法用在这里。② **配置权限白名单**（`.claude/settings.local.json`，原先只有 `Bash(git --version)` 一条，跑任何脚本都要人工点允许）：收录本项目四个脚本的 `python3` 调用、只读 git 命令（status / diff / log / show / branch / remote / ls-files / rev-parse / rev-list / check-ignore / blame）、`ls`/`grep`/`rg`/`wc`/`du`/`file`/`which`，以及 Step 2.5 专名核对每轮都要用的 `WebSearch`。**写操作一律不收录**——`git commit`/`push`/`reset`、`rm`、`find`（可 `-exec`）等仍逐次询问。该文件随 `.claude/` 被 gitignore，换机器需重配，已在 PLAYBOOK 环境配置里写明。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的。
- **2026-08-03（二）**：经用户确认，第二轮优化——**砍掉 Obsidian 残留、补上工程基建**。**升级模块**：删除 `tags.json` 与 `fix_format.py`、新增 `http_utils.py` 与 `tests/`、`notion_upload.py`、`notion_read.py`、`fetch_transcript.py`、`CLAUDE.md`、`PLAYBOOK.md`、`README.md`、`skills/dialogue_transcript.md`、`skills/illustrated_deepdive.md`。具体变更：① **删除 `tags.json` 及全部标签 / 双链规则**——经用户决策"Notion 里的标签不需要拆得很细"：成品 `.md` 不再写 YAML frontmatter，正文不再打 Obsidian 双链 `[[ ]]`。二者都是 2026-06-28 已移除的 Obsidian 同步工作流的残留：frontmatter 标签既不进 Notion（`create_page` 的 Category 固定填「播客访谈」，经用户确认保持现状）、也没有别的消费方；双链上传时还得专门剥方括号。同步清掉 CLAUDE.md Step 3 的标签与双链条款（原 1、2 项合并为一条"不写不打"的禁令并重新编号）、`dialogue_transcript.md` 的输出结构由 7 段减为 6 段（删掉「YAML Frontmatter」小节并重排编号）、`illustrated_deepdive.md` 输出语言里的双链条目、README / PLAYBOOK 的 `tags.json` 条目。`notion_upload.py` 的 frontmatter 跳过与双链剥离逻辑**保留**，用于兼容历史稿。② **删除死代码 `fix_format.py`**——硬编码指向早已不存在的 `output/Demis Hassabis｜.../…逐字稿.md`，全仓库无引用，README / PLAYBOOK 均未收录，跑一下就是 FileNotFoundError，却占着"执行层脚本"的位置误导接手者。③ **新增 `http_utils.py` 统一退避重试**——此前 `notion_read.py` 有自己的 8 次重试而 `notion_upload.py` 一次都没有（上传要发多批 blocks、暴露面更大反而更脆），`fetch_transcript.py` 也没有（免费源升为预判主力后，YouTube 限流概率上升）。现三个脚本共用一套策略：网络异常与 429/5xx 指数退避重试（429 优先听 `Retry-After`），其余 4xx 立即返回不空转。④ **新增 callout 支持**——`📍` 路线条、`💡` 说人话保底句、`🔍` 名词解释灰框（含 `> 🔍 …` 写法）转 Notion callout block（灰底 + 对应 emoji 图标），换行续写并进同一块；此前它们全部落成无层级的普通段落，skill 里规定的减负装置到了 Notion 就消失了。实测已归档的 Thariq & Cat Wu 那篇能识别出 8 个 callout。⑤ **新增 `tests/` 回归测试**（4 组 38 项，纯标准库、不依赖 pytest，入口 `python3 tests/run_all.py`）——覆盖 Notion 查重、字幕完整度闸门与源优先级、Markdown → Notion 转换、HTTP 重试。这几处修的都是"不报错、只是悄悄出错"的 bug，没有测试钉住极易回归。CLAUDE.md Step 6 自检清单新增"动过 .py 必须跑测试并报告结果"，PLAYBOOK 同步。⑥ **PLAYBOOK 补一条数据安全提示**——`output/` 是三件套的唯一本地副本（Notion 只有成品，`transcript.json` 与交接文档只在本地且不进 Git），别当临时目录清理。触发原因：用户复检时确认历史稿件的三件套已在此前清理中删除，字幕若要恢复得重抓、花配额且老视频可能已无字幕。遗留：Notion 表格块仍未支持；观察稿（模式 C）的归档 / 上传路径仍是断的——skill 无归档章节，而 `notion_upload.py` 按视频 URL 查重，观察稿没有单一 URL。
- **2026-08-03**：经用户确认，对工作流做一轮**体检式修复与优化**（脚本 bug + 省配额 + 文档漂移）。**升级模块**：`notion_upload.py`、`fetch_transcript.py`、`CLAUDE.md`、`PLAYBOOK.md`、`README.md`、`AGENTS.md`、`.gitignore`、`requirements.txt`、`docs/workflow.svg`、`full_demo.png`。具体变更：① **恢复被误删的 `requirements.txt` 与 `docs/workflow.svg`**（PLAYBOOK 的 setup 第一步与流程图链接都指着它们、已成死链），并给 requirements 补上一直漏写的 `youtube-transcript-api`（回退源依赖），svg 的 Step 2 路由补上观察稿。② **修复 Notion 查重对新形态失效**——`TYPE_SUFFIXES` 只认「图文精读/逐字稿/深度文章」，演讲实录与观察稿走到 `detect_type_suffix` 返回 None、`resolve_dedup` 被整个跳过，重传只会不断新建重复页；改为 `TYPE_NAMES` 四类 + 退役类，分隔符（`—`/` - `/` — `/`-`）统一归一化。③ **删掉 `infer_other_suffix` 的二元猜测**——它硬写死"非图文精读即逐字稿"，线上有无后缀旧页时会给它贴上猜错的后缀，正是历史上"内容是图文精读却被命名为—逐字稿"那类错页的成因；改为**页面标题一律自带类型后缀（含首次上传）、绝不推断**，遇到无后缀的历史页只告警、保留不动交人工。④ **补 `fetch_transcript.py` 的覆盖率 null 缺口**——原终判写作 `if duration and ...`，付费源整体失败或返回体缺 microformat 时 `duration` 为 None，完整度校验被短路跳过、半截字幕零校验放行（正是这个脚本存在的理由被绕过）；现新增 `get_video_duration()` 从 YouTube 观看页免费补抓 `lengthSeconds`，且**时长在换源判断之前就位**，仍拿不到则输出 `coverage_verified: false` + `warning` 并 stderr 告警，CLAUDE.md Step 1 同步新增"`coverage_verified` 为 false 禁止进入 Step 2"。⑤ **Markdown → Notion 转换器补齐缺口**——原先只认 `**加粗**`，代码块（封面 Recraft Prompt 就住在里面）、`![](url)` 图片、`[[双链]]`、行内链接与 `code` 全部以字面文本上墙；现支持围栏代码块（语言不在 Notion 枚举内降级 plain text）、http(s) 图片转 image block（本地路径降级为待补图提示，避免 400）、双链剥括号只留词条、行内链接转真链接；2000 字符上限改由 rich_text 内部按片切分，不再按字数硬切块（旧写法会把加粗从中间劈开），标题/引用/列表同受保护。⑥ **新增 `--prefer-free`**——免费源优先、覆盖率达标就不碰付费源（付费源免费额度仅 20 视频/月，而选题预判每条链接都要抓一次），残缺时仍回落付费源兜底；同时把回退源写死的 `languages=["en"]` 改为按"人工字幕优先、英文优先"选轨（写死 en 会让非英文视频两源皆废），输出新增 `language` 字段。CLAUDE.md 阶段 -1 与 PLAYBOOK 均已规定预判一律用该开关。⑦ **上传失败不再留空壳页**——`append_blocks` 改为抛异常，main 捕获后归档刚创建的空页再退出（空壳页会被下次查重误判成"旧版本"）。⑧ **文档漂移与仓库卫生**：`AGENTS.md` 已与 `CLAUDE.md` 漂移 65 行（还停在"两种输出模式"、无模式 C），改为指向 `CLAUDE.md` 的**符号链接**，从机制上杜绝双入口漂移；README 文件结构树补全 6 个 skill / docs / transcripts_pending / 交接文档，工作流步骤改为 6 步并修正"logs 不进 Git"的错误表述（实际是已跟踪文件）；`.gitignore` 删掉不生效的 `logs/`、补上 `transcripts_pending/`（该目录文件已按视频原标题重命名，不再匹配 `transcript_*.json`，432K 字幕正裸奔）；`full_demo.png` 由 2940px/9.8MB 压到 1102px/2.7MB。触发原因：用户要求盘点工作流可修复项，逐项体检后发现上述 8 类问题，其中②③④为会静默产出错误结果的真 bug。遗留：Notion 表格块尚未支持（表格行现各自成段、不再被粘成一坨）；`full_demo.png` 旧大文件仍在 Git 历史中，需重写历史才能清除，未做。
- **2026-07-21（三）**：经用户确认，为「批量命名规则」补上**待用字幕的存放位置**。**升级模块**：`CLAUDE.md`（阶段 -1「批量命名规则」、Step 1 复用规则）。具体变更：① 规则由三段扩为四段——预判交付后**所有**文件按视频原标题重命名（原写"留用的重命名、未选用的同样重命名"，措辞含混，已统一）；**未选用的一律移入 `transcripts_pending/`（没有则新建）、不删除**，理由写明"暂缓 ≠ 作废，重抓要花钱且可能已失效"，并确立"项目根目录只放脚本与文档、不留散落字幕"；进 Step 3 时再从 `transcripts_pending/` 移进 `output/<标题>/` 并改名。② 新增两条附注：**开工前按 `video_id` 字段确认**（文件名已被重命名过两轮，凭名字猜最易出错）；**交接文档联动**——移动字幕后必须同步更新引用了旧路径的交接文档，呼应 Step 4.5 同步铁律。③ Step 1 复用规则同步补上 `transcripts_pending/` 这条来源路径。触发原因：本轮清理仓库时把 5 份待用字幕（系列中下篇素材 2 份 + 暂缓的 Kimi 3 份）从根目录移入 `transcripts_pending/`，随即发现上篇的交接文档里"保留在仓库根目录"一句已脱钩、会误导接手 Agent 去重抓；原规则只说"保留不删除"、未指定位置，遂补齐。
- **2026-07-21（二）**：经用户确认（Ratchet Check），为模式 B 精编压缩新增**「删减红线：时间轴缺口扫描」**强制步骤。**升级模块**：`skills/dialogue_transcript.md`（「面向发布的精编压缩」段落末尾）。具体变更：新增一节，规定精编压缩完成后、进入 Step 2.5 之前必须做时间轴缺口扫描——① 按成品时间戳顺序扫出**所有被删的连续 ≥2 分钟区间**；② 逐区间写明删除理由，且理由**只能落在四类**（广告口播 / 铺垫性长问 / 重复论证 / 纯附和回合）；③ **凡不属于这四类的一律补回**，不接受"篇幅太长""不够重要"；④ 明确**删减改变立场强度的等同硬伤**（嘉宾的让步、限定、自我收口被删会让观点比原话更硬，属扭曲，必须补回）；⑤ 扫描结果按固定格式写进交接文档的「✅已完成」。并附实例教训。触发原因：本轮 Elizabeth Stone（Lenny's Podcast，72 分钟）逐字稿精编初稿**静默删掉了 [50:22–52:54] 整段 2.5 分钟关键问答**（全片唯一正面回答"AI 实验室抢人时 Netflix 凭什么"、且是该篇标题主线的正面对照），同轮还删掉了她对 AI 生成内容的让步段（使立场比原话更硬）；成品读起来依然连贯，初稿自检完全没有察觉，两处均靠 Step 2.6 Agent Council 才捞回。原 skill 只写了"删什么/留什么"，缺少**压缩之后的反向校验**这一环，遂补上。
- **2026-07-21**：经用户确认（Ratchet Check），新增**多链接批量选题预判的字幕文件命名规则**。**升级模块**：`CLAUDE.md`（阶段 -1、Step 1 复用规则）。具体变更：① **`阶段 -1`** 新增「**批量命名规则（多条链接时强制）**」三段式——**抓取阶段**一律输出 `transcript_temp_<video_id>.json`（一视频一文件、可并行，**禁止**多视频共用同一个 `transcript_temp.json`）；**预判交付后**把文件按 **YouTube 视频原标题**重命名（`<视频原标题>.json`，非法字符 `/ : |` 替换为 `-` 或空格、不用下划线），未选用的同样重命名保留不删除；**进 Step 3 归档时**再按既有规则统一改为 `transcript.json` 或 `transcript_<来源>.json`。② **`Step 1 复用规则`** 补充批量分支：复用阶段 -1 的 `transcript_temp_<video_id>.json` 或已按原标题重命名的那份，并要求**开工前按 JSON 的 `video_id` 字段确认拿到的是本次要做的那条、不要凭文件名猜**。触发原因：本轮用户一次发来 6 条链接（Netflix CPTO / Kimi K3 ×3 / Thariq ×2）要求批量选题预判，而 CLAUDE.md 只规定了单一 `transcript_temp.json`，六条并行会互相覆盖；本次自行改用 per-video 命名绕过（属未写入规范的降级路径），且用户随后另行要求"把字幕文件名改成对应的 YouTube 视频名称以便区分"，两点合并沉淀为永久规则。
- **2026-07-10**：经用户决策，新增**第四种文体「观察 / 观点类稿」**并接入路由（Step 6 文档进化）。**升级模块**：新增 `skills/observation_commentary.md`、更新 `CLAUDE.md`。具体变更：① **新增 `skills/observation_commentary.md`**——定义"观察 / 观点类稿"生成规范：把**多个一手信源**（访谈 / 播客的 Summary 与重点、研报、新闻，及用户提供的数据 / 图片 / 截图）**编织成一条自己的中心判断线**，而非单一视频转写（"只想还原一个视频→走逐字稿"已在文中写死分流）；**五条核心铁律**＝先立判断线再动笔 / 编织而非拼接 / 冷峻侧写去情绪 / 主动去公关化点破各方利益立场 / 引语逐字数字可核；**信源不设死数量**（两三个到七八个皆可，重"一手 + 可信 + 能相互印证或对撞"）；**结构模板**＝症状（引爆点，非强制但推荐，须立刻转正题）→ 人话入口 → 立判断线 → 多环观察（每环＝一信源 + 一张证据图 + 一句加粗判断）→ 平衡 / 照镜子 → 落点（与开头比喻咬合）；另含加粗规范（只给判断句、每 beat 至多一处）、图＝证据非插画、事实严谨（引语核到出处 / 数字口径存疑标来源或软化 / 信源观点署名"据 X"）、摘要 ≤20 字、封面用场景隐喻不上字，及与逐字稿 / 图文精读的一句话路由区分表。② **`CLAUDE.md → 工作流路由规则`** 由"两种模式"扩为"三种模式"，新增**模式 C（观察 / 观点稿）**：当用户给的是"一个引爆事件 + 多个一手信源、要一个行业判断"（而非还原某一个视频）时走 `observation_commentary.md`，不走单一视频抓取流程。触发原因：本轮 Palantir / Karp 选题历经十余轮人机协作，最终由 Leader 定稿为"基于 Karp 的 CNBC 发言 + All-In 播客 + Dylan Patel × 红杉 等多个一手信源编织的行业观察稿"，其"多源编织一条判断线、冷峻去情绪、判断句加粗、主动去公关"的写法与图文精读 / 逐字稿均不同，遂沉淀为第四种可复用文体。
- **2026-06-28**：经用户决策，**彻底移除 Obsidian / Second Brain 同步工作流**（Step 6 文档进化）。**升级模块**：`CLAUDE.md`、`AGENTS.md`、`README.md`、`PLAYBOOK.md`。具体变更：① `CLAUDE.md`、`AGENTS.md` 的 Step 4 标题由「上传 Notion 与同步 Second Brain」改为「上传 Notion」，删除"复制到 `/Users/jialiwu/Desktop/Second Brain/YouTube Transcripts/`"子步骤及其 ✅/❌ 输出信号——工作流不再做双向同步、只上传 Notion；② `README.md` 删去第 6 步「同步 Obsidian」、首段「归档至 Notion 与 Obsidian」改为「归档至 Notion」；③ `PLAYBOOK.md`「Notion / Obsidian 同步」改为「Notion 同步」。**保留**：`[[ ]]` 双向链接写作约定（仅作正文标记知识节点用，与文件同步无关，CLAUDE.md/AGENTS.md/illustrated_deepdive.md 中三处 Obsidian 提及均为此）。触发原因：换机后 `~/Desktop/Second Brain` 在本机始终不存在，同步连续多次失败（见 `workflow_execution.md` 06-13 / 06-22 / 06-27 三条），用户决定彻底删除该步骤而非继续修路径。
- **2026-06-28（二）**：经用户决策，固化「归档三件套 + 交接文档」机制（Step 6 文档进化）。**升级模块**：新增 `skills/handoff_doc.md`，更新 `CLAUDE.md`。具体变更：① **新增 `skills/handoff_doc.md`**——定义"交接文档（上下文状态快照）"的固定模板与两条铁律（每篇必出、必须随每轮精修同步更新），模板区块＝顶部「当前阶段 + 最后更新 + 阅读顺序」→ 任务背景(形态/来源/编辑决策) → 关键文件+Notion链接 → ✅已完成 → ⏳待办(⚠️专名/🔵润色/🖼️媒体/精修方向) → 给接手Agent一句话；目的是让"新对话/新 Agent 只读这一页即可恢复工作记忆、不必回吞整轮生成过程"，把生成与精修两阶段的上下文解耦、大幅省 token。② **`CLAUDE.md → Step 3`** 扩为"归档三件套"：成品 `<标题> - <形态>.md` + `transcript.json`（合辑改名 `transcript_<来源>.json`）+ **强制生成 `交接文档.md`**（按 handoff_doc.md 模板），并把演讲实录形态的文件命名补进保存规则；输出信号改为"已归档三件套"。③ **`CLAUDE.md → Step 4.5`** 新增**交接文档同步铁律**：每完成一轮精修/定稿后必须同步更新交接文档的「当前阶段/✅已完成/⏳待办」，防止成品与交接文档脱钩误导接手 Agent。触发原因：用户提出"初审出初稿即提交、同时在文件夹生成 成品.md + 字幕.json + 交接文档.md，以便重开新对话让新 Agent 据交接文档接续"，经评估对 token/上下文管理收益显著，遂固化。
- **2026-06-28（一）**：经用户决策，从项目中整体移除「Obsidian 同步」工作流（Step 6 文档进化）。**升级模块**：`CLAUDE.md`、`AGENTS.md`、`README.md`、`PLAYBOOK.md`。具体变更：① `CLAUDE.md` 与 `AGENTS.md` 的 **Step 4** 由"上传 Notion 与同步 Second Brain"改为"上传 Notion"，删除"复制到 `/Users/jialiwu/Desktop/Second Brain/YouTube Transcripts/`"子步骤及其 `✅ Obsidian 同步成功` 输出信号；② `README.md` 删除特性描述"并自动归档至 Obsidian"与流程第 6 步"同步 Obsidian"；③ `PLAYBOOK.md` 将"Notion / Obsidian 同步"改为"Notion 同步"。**保留项**：正文里用 `[[ ]]` 双向链接标记知识节点的写作约定不动（属 Markdown 写作约定、与同步无关）。触发原因：换机后本机始终无 `~/Desktop/Second Brain` vault，Obsidian 同步连续多轮失败、用户每轮均选择跳过，遂彻底移除该工作流而非继续保留死步骤（呼应 06-13/06-22 多次撞库的 Ratchet 提示，本次按用户指令一次性清除）。
- **2026-06-27**：经用户决策，新增「单人演讲实录（精修版 + 配图）」第三种产物形态（Step 6 文档进化）。**升级模块**：`skills/dialogue_transcript.md`。具体变更：在「核心定位」后新增**「形态变体：单人演讲实录」**一节——适用单人演讲 / keynote / 大会分享，以演讲者第一人称原话为正文（保留语气与金句、清口水词、理顺跑题、按幻灯片分段锚定并插 PPT 截图占位、不重构主题顺序），是介于「图文精读」（重构）与「对话体逐字稿」（多人还原）之间的第三态；无需 `>>` 说话人切换核对，其余规范（导读层 / 去 AI 味 / 审稿人自检 / 多轮精修 / 外部素材 / Step 2.5 核对）全部沿用；文件命名 `<标题> - 演讲实录.md`。并定义**复合用法「现场回闪」**：同一场大会多位嘉宾就同一问题分享时，可把多篇演讲实录并入一篇，开篇价值段交代"某大会现场 N 位不同领域嘉宾回答同一问题"、正文以「上篇〈视角 A〉— 嘉宾1 / 下篇〈视角 B〉— 嘉宾2」分节承载，共享总标题与导语。触发原因：本轮规划 Cursor Compile 26 的 Ryo Lu（设计师视角）与 Farhan Thawar（工程负责人视角）两场演讲，确定用"演讲者逐字稿精修 + PPT 截图"呈现、并合并成「AI 时代，你的工作还剩什么？」现场回闪一篇，遂将该形态固化进规范。
- **2026-06-26（三）**：经用户确认，将封面图生成固化为「两步流程」（Step 6 文档进化）。**升级模块**：`skills/illustrated_deepdive.md`（第 3 节封面图，模式 A / B 通用）。具体变更：把原"单步 Recraft 封面 prompt"改写为**两步流水线**——① **Recraft AI 定背景与整体风格**：Style=Motion Blur、Size=2048×1024、`no text`，按「十字路口」house style（暗冷电影感 + 横向 amber 光轨 + 顶部/中央留干净暗区），给出可复用英文 prompt 模板（`{概念}` 槽）；② **Google AI Studio · Nano Banana 最新模型叠主题词 + 调细节**：主题词放顶部或中央、走 house 细体 light-weight 白字 + 字距 + 微光，按需把屏幕换成 Claude Code 的 CUI / 终端界面、加 Claude 八角星 logo。**硬性要求：最终成图 2048×1024、高清。** 仍守"第一步 no text、第二步才叠字"的铁律。触发原因：本轮 Fiona Fung 封面经多轮打样，定型为"Recraft 出底图 → Nano Banana 叠字调细节"的稳定流水线，固化以便复用。
- **2026-06-26（二）**：经用户确认，将本次给「对话体逐字稿」做的发布级优化择要移植到「图文精读」（Step 6 文档进化）。**升级模块**：`skills/illustrated_deepdive.md`、`skills/reader_facing_review.md`。具体变更：① `illustrated_deepdive.md` 新增**面向发布的多轮精修流程**（外部素材并入 → 正文叙事章节重构/合并/删减/调序 + 路线条/减负装置/媒体占位 → 非正文：内容与价值概括→嘉宾背景→核心观点→收束段 → 最后 5 标题+摘要钩子+封面 Prompt）+ **外部素材处理表**（推特/文章/官方页/他人解读/嘉宾其他演讲一律标来源、与嘉宾观点/字幕内容区分、无据标 ⚠️、第三方标明；幻灯片走媒体占位）+ **借 takeaways 校准标题/价值**。② `reader_facing_review.md` 新增**「模式 A（图文精读）专用复核清单」**（9 段式完整/双锚点都在/价值概括与嘉宾背景零重复/路线条/减负装置/章节叙事化且与正文对应/核心观点成逻辑链/媒体占位/收束段两面判断），供 Agent Council 评审 Agent 按模式切换打分维度。说明：框架级优化（阶段 -1 选题预判、Agent Council 两关口、`notion_read.py` 终校、双锚点、核心观点 punchy 化）本就模式无关或图文精读已具备，无需重复移植。
- **2026-06-26**：经用户决策，将本轮 Fiona Fung 逐字稿协作中沉淀的「选题预判 + Agent Council 双关口 + 多轮精修流程 + 外部素材处理」固化进规范（Step 6 文档进化）。**升级模块**：新增 `skills/topic_assessment.md`、新增脚本 `notion_read.py`，更新 `CLAUDE.md`、`skills/reader_facing_review.md`、`skills/dialogue_transcript.md`。具体变更：① **新增阶段 -1 选题预判**（`CLAUDE.md` + `skills/topic_assessment.md`）：用户只发链接、未说"开始转录"时，先以编辑视角做四维等权加权打分（话题传播度 / 内容可沉淀性 / 可扩展性 / 独家一手性，满分 10）+ 受众/定位描述 + 制作成本与可视化可行性标注，给结论后停住等"开始转录"。② **新增两个 Agent Council 关口**（`CLAUDE.md` Step 2.6 交付前自检、Step 4.5 定稿前终审；机制写入 `skills/reader_facing_review.md`）：评审 Agent 打分出清单 → 核实 Agent 拿完整正文逐条核实 → 主模型综合共识；两条铁律——必须全新 Agent（模型不能自评）、必须喂完整正文（压缩摘要会制造"可读性崩塌"假阳性）。③ **新增多轮精修固定顺序 + 外部素材处理表 + 标题钩子规则**（`skills/dialogue_transcript.md`）：精修顺序＝外部素材并入 → 正文(结构/合并/删减/调序) → 非正文(内容与价值→嘉宾背景→核心观点→写在最后) → 最后标题+钩子；外部素材（推特/文章/官方页/他人解读/嘉宾其他演讲）一律当"外部事实"、用截图/视频/图注呈现、不混入对话冒充原话、不用灰框；标题 ≥5 个、钩子 ≤20 字。④ **新增 `notion_read.py`**：用 .env 集成把已上传/已编辑的 Notion 页读回本地做终校 / Council 复核（附"页面移出上传数据库后集成 404、需重新连接集成"的坑）。触发原因：本轮 Fiona Fung 逐字稿经历十余轮人机协作精修，暴露出旧规范缺「选题前置判断、交付前/定稿前互审、精修顺序、外部素材如何并入」四块，遂沉淀成可复用框架。
- **2026-06-17**：触发 Ratchet Check（Step 5.5），经用户确认修复字幕抓取的「静默半截」缺陷。**升级模块**：`fetch_transcript.py`、`CLAUDE.md`。触发原因：抓取 Bloomberg《The Circuit》Anthropic 专题（真实时长 47:40）时，付费源 `youtube-transcript.io` 只返回到 23:43（覆盖率 50%）且不报任何错，导致基于半部片子做出错误的内容评估，经用户指出才发现。具体变更：① `fetch_transcript.py` 新增**完整度校验**——用视频 `lengthSeconds` 与字幕末段时间戳算覆盖率，< 90% 即判残缺并**自动回退到 `youtube-transcript-api` 直连 YouTube 重抓**，两源都不达标则以 error 退出（exit 1）；输出 JSON 增加 `duration_seconds` / `last_timestamp_seconds` / `coverage` / `source` 字段。② `CLAUDE.md → Step 1` 新增**字幕完整度校验铁律**：进入 Step 2 前必须查看 `coverage` 并向用户报告，`coverage < 0.9` 或脚本非零退出时禁止进入 Step 2；成功简报格式加上覆盖率与来源。
- **2026-06-16**：经用户决策，将「专名核对铁律」沉淀进规范、并做成单一出处（Step 6 文档进化）。**升级模块**：`CLAUDE.md`（主出处）、`skills/illustrated_deepdive.md`（改为一行引用指回）。具体变更：在 `CLAUDE.md → Step 2.5` 新增第 2 条**专名核对铁律（外文名 = 高风险区）**（其余条目顺延为 3–6），适用模式 A / B：公司 / 产品 / 模型 / 论文 / 人名等外文专名自动字幕极易听错（实例 `AMI Labs`→"Omni/Ammy Laps"、`LeWorldModel`→"layworld model"），处理顺序固定为①能用 WebSearch/WebFetch 核到官方写法的须先查证再定稿、不照搬音译猜测；②查不到或没把握的显式标 `⚠️建议人工核对` 交用户、绝不臆造"看着像"的名字；③字幕外作者自补的背景事实（年份/头衔/"谁最早提出 X"）无据同样标注、不默认采信。`skills/illustrated_deepdive.md` 的「对照字幕核校」段仅保留一行摘要并指回 CLAUDE.md，避免规则双写漂移。触发原因：LeCun VLA 下集复核中，`AMI Labs` 与 `LeWorldModel` 两处外文专名此前均因照搬字幕音译而写错，暴露"专名拿不准时擅自断定"的风险。
- **2026-06-13**：触发 Ratchet Check（Step 5.5），经用户确认修正换新电脑后的硬编码路径。**升级模块**：`PLAYBOOK.md`、`CLAUDE.md`、`AGENTS.md`。具体变更：将三处写死的旧用户名 `shirleywu` 路径改为新机实际用户名 `jialiwu`——① `PLAYBOOK.md`「使用方法」中的 `cd` 示例路径（同时改为新机实际项目目录名 `youtube transcript scraper and summary`）；②③ `CLAUDE.md` 与 `AGENTS.md` 的 Step 4.2 Obsidian 第二大脑目录 `/Users/shirleywu/Desktop/Second Brain/...` → `/Users/jialiwu/Desktop/Second Brain/...`。触发原因：换新电脑（用户名 jialiwu）从 GitHub 克隆工作流后首次运行，发现文档与脚本仍残留上一台机器的硬编码绝对路径，若启用 Obsidian 同步会因路径不存在而失败。
- **2026-06-11**：经用户决策，将 Sergey Levine 逐字稿精编实践沉淀进规范（Step 6 文档进化）。**升级模块**：`skills/dialogue_transcript.md`、`skills/illustrated_deepdive.md`。具体变更：①**新增「面向发布的精编压缩」**：谈话类逐字稿面向公众号发布时，在保留核心内容前提下压缩（删铺垫长问 / 附和回合 / 广告口播 / 重复论证，留关键问答与例子金句并尽量保原话，合并相邻同主题小节约 8 节，区分完整存档版与发布精编版、后者大节改名「访谈正文（精编）」）；同步修订「不删除看似不重要的对话」旧规则。②**核心观点标题改版**：标题直接写成钩子 / 观点句，禁用 `概念名｜描述`/`概念名：描述` 拼接结构、去掉数字编号，标题下用一两句精简概括（压缩篇幅但保住 VLA / 瓶颈 / 激活能等核心信号）；illustrated 与 dialogue 两文件同步对齐。③**逐字稿章节标题**：要求短且具体、点出核心，禁用空泛悬念式标题（反例「说到底，只有一个问题」）。④**新增「去 AI 味」自检**（dialogue 此前无）：尽量不用破折号、少用「不是…而是…」、禁用包装词 / 空转连接；illustrated「去 AI 味」清单补「尽量不用破折号」一条。⑤文档审查顺带修正 `PLAYBOOK.md`「自定义修改」中『8 段式』旧笔误为『9 段式』（与 06-09 升级对齐）；经审查 `CLAUDE.md`、`AGENTS.md`、`README.md` 本轮无需变更（skill 层改动已被高层文档以引用方式封装）。
- **2026-06-10**：触发 Ratchet Check（Step 5.5），经用户确认写入两条永久规则（源于 Sergey Levine·ILTB 双稿复核中发现的两类硬伤）。①`glossary.md`【易混译法】新增条目：steel manning / steel man → **钢人辩护（最强版辩护）**，严禁译作"稻草人"（straw man 稻草人谬误为其反义词）；触发原因：图文精读把 Levine 原话 "steel manning" 误写成"稻草人辩护"，且与全文"稻草人困局"开篇形成反义混用。②`skills/dialogue_transcript.md`【说话人识别规则】第 2 条强化：字幕 `>>` 换人标记须逐一核对，短插话（接话/替对方补完半句/吐槽）必须单独归属其人、不得因语义连贯并入长答主，下一句以 "Exactly / 没错" 开头是上句换人的信号；触发原因：逐字稿把 Patrick 的"第 37 手"接话误并入 Sergey 的长答。
- **2026-06-09**：经用户决策，将 LeCun 上下集图文稿的精修经验沉淀进规范（三文件联动，保持一致）。**升级模块**：`skills/illustrated_deepdive.md`、`CLAUDE.md`、`glossary.md`。具体变更：①**结构 8 段 → 9 段**：新增「结尾收束段」，与「内容与价值概括」并列为**强制双锚点**（开头给"看什么/为何读"、结尾给"判断+开放结局"，替代"把核心观点砸开头"，治长文"读完没收获感"）。②**新增「长文减负装置」**（6000 字硬核稿必备）：路线图句（开头）、一句话路标（中途 2-3 处）、"说人话"保底句（硬核段），各附实例；并明确"路线条（全局）+路标（局部）+收束段（收尾）"三件套构成导览系统。③**第 4 段新增「路线条」导航条**要求（`📍 本文路线：A→B→C` callout，末项用中性词、忌"赌局比分"这类预设输赢/抽象词）。④**新增「系列上下集处理规范」**（嘉宾背景跨集一字不改、上抛下接的承接句、层级线索贯穿、双向回链、路线条各集独立）。⑤**新增「去 AI 味」强制自检**（禁用包装词/空转连接清单 + "用具体事实代替形容词"原则；保持编辑语感而非大白话）。⑥**核心观点段**：按用户决定**完整保留**，仅补"默认保留、不擅自删除"注记。⑦**CLAUDE.md 对齐**：模式 A 结构 8 段式 → 9 段式、删旧词"时间戳重构正文"→"重构正文"、补双锚点与减负装置说明。⑧**glossary 补条**：锁定 VL-JEPA（弃 VLJA）、𝝅0.7（弃 PI-07）、技术栈（弃堆栈），增末端执行器/CEM/现象学模型。**封面 Recraft prompt 与核心观点结构按用户要求不动。**
- **2026-06-05**：经用户决策，对工作流做结构性重构（受 LeCun Part 1 图文稿实践驱动）。**升级模块**：`CLAUDE.md`、`skills/`、新增 `glossary.md`。具体变更：①**产物收敛为两种**——砍掉「深度文章」，删除 `skills/article_generation.md`；最终只保留「图文精读稿」（模式 A 默认）与「对话体逐字稿」（模式 B）。②**新增主力规范** `skills/illustrated_deepdive.md`：定义图文精读稿 8 段式结构（标题5选/摘要钩子/封面Recraft Prompt/内容与价值概括/嘉宾背景/核心观点/重构正文/系列回链），明确"价值概括只讲事、嘉宾背景只讲人、二者零重复"，核心观点按时长分档（30–40 分钟约 5 条、1 小时 5–8 条且成逻辑链），正文不再自标时间戳（直接对照原始字幕+逐字稿重构），叙事性标题+密集媒体占位。③**新增全局 Step 2.5「对照字幕核校」**（生成后、保存前，模式 A/B 通用，定为工作流核心）：逐板块核对事实/数字/专名/因果，区分必改硬伤与可选润色，对照术语表统一译法，每批给"✅无硬伤/⚠️N处硬伤"判定。④**新增** `glossary.md` 项目术语对照表（嵌入向量/网络-编码器/JEPA全称/V-JEPA 2 连字符/其他-其它等），供 Step 2.5 跨篇统一。⑤同步修正 CLAUDE.md 路由规则、Step 2/3 文件命名（`- 图文精读.md`）、Step 6 自检清单、角色定位句，并清理 `dialogue_transcript.md` 中指向已删文件的悬空引用。
- **2026-05-30**：触发 Ratchet Check（Step 5.5），经用户确认向 `CLAUDE.md` 写入两条永久规则。①【Step 4】**Notion 重传查重规则**：更新后重传一篇已上传过的文章前，必须先 `find_pages_by_url` 查询该 URL 现有页面，发现命名与文件名后缀不符的历史页（如深度文章被误命名为「—逐字稿」）须先归档再上传，避免脚本误判为"另一种类型"而新建重复页；上传后复查确认每种类型仅存一页。触发原因：本轮更新 Hassabis 对照文重传时，因历史遗留错误命名页导致脚本新建出重复页，事后归档幽灵页才修复。②【Step 5】**写日志前先读格式规则**：向 `logs/workflow_execution.md` 追加记录前必须先 Read 该文件、确认其当前实际格式（单行 bullet、倒序、最新在顶），不得凭假设的标题/结构做 Edit。触发原因：此前两次写日志因假设了不存在的文件头格式导致 Edit 失败。
- **2026-05-19**：根据用户基于 Notion 范本（Jim Fan 那期）提出的结构升级建议，对 `skills/dialogue_transcript.md` 进行大幅重构。主要变更：①新增"开篇引言（blockquote）"作为首部钩子，要求一句来自嘉宾本人、最具判断力的金句；②新增"视频价值段"，定位为编辑视角的价值判断而非内容摘要（3-5 句上限）；③简化"嘉宾背景"至 1-2 句；④升级"核心观点"为命名化概念清单（每条必须是"有名字的"概念，而非描述性标题），条数无上限；⑤将逐字稿章节标题从功能性切换为叙事性（明确禁用"关于 X 的讨论"类标题）；⑥新增媒体占位符标准（🖼️ 图片占位、🎞️ GIF 占位）；⑦新增"翻译质量与受众平衡"章节（专业性 + 可读性双底线，照顾从业者与文科背景普通读者）；⑧新增完成后必须执行的"审稿人自检 Checklist"（A 嘉宾背景准确性 / B 开篇引言与视频价值 / C 核心观点完整性 / D 翻译准确性 / E 标题与内容对应）。
- **2026-05-12**：触发 Ratchet Check（Step 5.5），在 `skills/dialogue_transcript.md` 中补充和修正了逐字稿的格式规范：访谈信息统一使用中文冒号，核心观点数字列表标题加粗并提行正文，增加“以下为逐字稿正文：”二级标题，规范说话人标签后的单个空格。
- **2026-05-12**：优化 `skills/dialogue_transcript.md` 输出结构（移除内容概述和重点关注），合并原重点关注至核心观点，并放开核心观点 3-5 条的上限限制（长访谈可达 8-10 条）；同步在 `skills/article_generation.md` 放开条数限制。
# System Changelog (Meta-Harness)

> 此日志文件用于记录 `YouTube Transcript Scraper and Summary` 系统框架本身的演进与维护。
> 当触发 Meta-Harness 自检协议（Step 6）时，由 Agent 根据用户的批准在此记录：架构优化、规则重构、脚本升级及目录结构变动。

---

## [2026-05-06]
- **升级模块**：`CLAUDE.md`
- **变更详情**：
  1. **(Fix 1) Success Silence — 输出信号规范**：在 Step 1、Step 3、Step 4（Notion + Obsidian）各自新增"输出信号（Output Signal）"规范。成功路径仅输出一行简报，不返回 JSON、page ID 或完整 API 响应体；失败路径输出完整错误信息，等待用户确认后再处理。目标：降低 context 信噪比，防止 agent 因冗余确认信息失去焦点。
  2. **(Fix 2) Ratchet Check — 棘轮检查协议（Step 5.5）**：新增 Step 5.5，在每次工作流执行结束后自动触发轻量级自检。满足触发条件（运行异常、用户修正、降级路径）时，起草永久规则并向用户请求确认写入；用户确认后立即更新 CLAUDE.md 或 article_generation.md，并记录本 changelog。无异常时保持沉默。目标：将 failure-driven 规则积累变成系统性机制，而非依赖用户手动触发 Meta-Harness Protocol。

---

## [2026-04-23]
- **升级模块**：`CLAUDE.md`, `README.md`, `tags.json`, `notion_upload.py`, `skills/article_generation.md`
- **变更详情**：
  1. **(Phase 1) 上下文持久化基建**：引入 `tags.json` 强约束标签发散行为；建立 `logs/` 机制提升系统可观测性；打通 Obsidian `Second Brain` 的 Markdown 双向同步路径。
  2. **(Phase 2) 极简收纳与分类降级**：确立了“本地文件夹不带标签前缀、直接使用完整自然命名”的规则。将标签属性全面转移至标准的 YAML Frontmatter，并同步优化了 `notion_upload.py`，使其能自动跳过 YAML 块。
  3. **(Phase 3) 知识网分层治理**：确立了 **“宏观分类用 YAML Tags 强收敛，微观术语用 Obsidian 双链 `[[ ]]` 自由发散”** 的核心分类准则，并在生成规范中禁用了正文内的 `# 一级标题`。
  4. **(Phase 4) Meta-Harness 自进化协议**：在大纲内新增了触发式的自检 Checklist，允许系统在发生架构改动后主动提出更新 README 等文档的建议并记入本系统日志。
