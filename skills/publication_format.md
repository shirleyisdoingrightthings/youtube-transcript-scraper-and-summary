# 发布格式统一规范（Publication Format · 跨形态）

> **适用范围：全部产物形态**——图文精读（模式 A）、对话体逐字稿（模式 B）、观察 / 观点稿（模式 C）、单人演讲实录。
> **本文件是这些约定的唯一来源。** 各形态 skill 只写自己特有的部分，排版与共有区块一律以本文件为准；如有冲突，以本文件为准，并回头修掉那个 skill。
> 定型自 Notion 范本《Claude Code 工程负责人 Fiona Fung：如何打造全世界最 AI Native 的工程团队?》，经用户确认为跨形态通用。

---

## 一、共有区块与顺序

每一篇成品，无论哪种形态，都由「共有壳」+「形态主体」构成。共有壳的顺序不可调换。

> **哪些是硬性、哪些可合并**：`# 嘉宾背景`、`# 核心观点`、`# 写在最后` 三块**不是强制独立成节的**——当篇幅较短、或走「解读稿」这类高密度形态时，可以**合并进导语**（把「他是谁、凭什么」与「这篇讲了什么」压成开头的两三段），或与相邻区块合并。判据是读者能不能在开头三十秒内拿到「他是谁 / 值不值得读 / 能带走什么」，形式不重要。
> **但 `**本文来源**` 是硬性的，任何形态都不得省略或合并掉**——尤其当正文并入了外部素材且不做区分时，它是全篇唯一的追溯入口。

| 位置 | 区块 | 说明 |
|---|---|---|
| 开头 | **开篇金句**（blockquote） | 一句嘉宾原话，全文题眼 |
| | **主图占位** | |
| | **署名行** | `👩 编译: <名>` / `🧑‍🎨 排版: <名>`，两行紧挨、中间不空行 |
| | **导语**（无标题正文段） | 说清「这篇为什么现在值得读」＝嘉宾的独特位置 + 行业当下；**不复述履历** |
| | `# 嘉宾背景` | 以 `姓名｜职位` 开头；**须 WebSearch 补料、要有张力**；**不谈本篇为什么值得读** |
| | `# 核心观点` | **无序列表**，每条「**一句加粗判断句。** 一到三句展开」 |
| 中段 | **形态主体** | 各形态自有：逐字稿走对话章节、图文精读走重构正文，等等 |
| 结尾 | `# 写在最后` | 收束段，**每篇必出** |
| | `**本文来源**` | 元数据 + 外部素材声明，文末粗体小节 |

**开篇金句是题眼，要出现三次**：标题下、导语末尾、「写在最后」。选句时先想它能不能撑起结尾——撑不起就换一句。

### 生成顺序（强制 · 不只适用于精修，初稿同样按此）

**正文 → 核心观点 → 导语 + 嘉宾背景 → 标题与钩子。**

理由：核心观点是从正文里提炼的，正文没定就提炼不准；导语要说清「这篇为什么值得读」，得先知道正文与核心观点最后长什么样。**倒着做必然返工**，而且会出现核心观点覆盖不到后半场、导语承诺了正文没兑现的东西这类错。标题与钩子永远最后做。

### 嘉宾背景（`# 嘉宾背景`）

一到两段，以 `姓名｜职位` 开头（全角竖线），然后接履历。**职责是让读者觉得「这个人值得听」，所以要有张力、不能写成简历条目。**

- **必须先 WebSearch 补料，不能只用字幕里那点自述。** 嘉宾谈自己往往轻描淡写，光靠字幕写出来的背景平淡无味，读者没有读下去的欲望。
- **优先找这三类硬料**：① **可量化的战绩**（投了多少、变成了多少、出自多大的基金——数字比形容词有力得多）；② **有戏剧性的关键动作**（主导过什么、推翻过谁、赌对过什么）；③ **「提前喊过」的记录**（他此前在哪次拐点前做过判断、后来兑现了没有）。第三类最能建立本篇的可信度，也最容易被漏掉。
- **所有事实必须可核实**，来自公开报道或官方页面；核不到的一律不写，**不得为了张力编造或夸大**。拿不准的标 `⚠️建议人工核对`。
- **不承担「这篇为什么值得读」**，那是导语的活。

### 导语（无标题正文段）

紧跟署名行，**说清这篇编译为什么值得期待与关注**。这是编辑视角的判断，不是内容摘要。

- **要结合两样东西**：① 嘉宾的独特位置（凭什么是他来说这件事）；② **当前的行业趋势 / 时间点**——用可核实的当下数据或态势，把「这篇为什么是现在读」钉住。行业数据同样要 WebSearch 核实，并在文中交代口径来源。
- 点出这场对话真正的分量在哪，**末尾落回开篇金句**，让题眼第二次出现。
- 不要写「这期视频讲了 X、Y、Z」，那是核心观点的工作。

### 嘉宾背景与导语：零重叠（强制）

这两块最容易撞车——都想讲「他是谁、他多厉害」。分工必须干净：

| | 嘉宾背景 | 导语 |
|---|---|---|
| 回答什么 | **他是谁、凭什么** | **这篇为什么现在值得读** |
| 装什么 | 履历、战绩、过往判断记录 | 行业当下 + 这场对话的独特分量 + 本篇主线 |
| 不许装 | 本篇讲了什么、为什么值得读 | 复述履历与代表作 |

**自检方法**：把两段里的**专有事实**（公司名、代表作、可量化战绩、关键年份事件）各列一份，**两份清单不应有交集**。有交集就是重叠了，从导语里删，履历只在嘉宾背景出现。

- 比的是**同一个事实**，不是同一个数字。导语写「数据截至 2026 年上半年」、嘉宾背景写「2026 年 2 月出版」，年份相同但事实不同，**不算重叠**；只按数字比会误报。
- **典型的真重叠**是「提前喊过」那类记录被写两遍。正确分工：**导语只下判断、不带证据**（「上一次他这样开口，是在上一轮估值调整之前」），**嘉宾背景供全证据**（哪一年、在哪说的、写了什么、后来兑现没有）。

### 三块的篇幅上限（强制）

这三块是读者最先看到的内容，**必须准确、且不能啰嗦**：

- **嘉宾背景**：**最多一段，两三句话**。只留最硬的战绩与「提前喊过」的记录，其余代表作、履历细节一律砍掉。
- **导语**：一段，**不超过 5 句**。
- **核心观点**：每条「一句加粗判断句 + 一到两句展开」，展开部分**不得复述判断句**。

**这三块尤其要清空转折废话**：「这句话不是修辞，而是……」「真正的分量不在 A，而在 B」「这不是 X，是 Y」这类句式，读起来像在铺垫，实际不传递任何信息，删掉之后意思不变就是废话。

**`# 写在最后` 的写法**：先承认这场对话没给出标准答案的那些问题，再把开篇金句第三次抬出来，然后用两三句讲清"能带走什么"（落在**具体行为**上，不是形容词），最后给编辑自己的判断、可留开放结局、不预设输赢。这是编辑的话，不写成引语，也**不得替嘉宾总结出他没表达过的立场**。

**`**本文来源**` 必含**：主源（原标题 / 节目 / 播出时间 / 主持人 / 链接）、外部素材来源（如有）、一句成品说明（做过哪些清理）、外部素材声明（如有，要求见各形态 skill 的「外部素材处理」）。

---

## 二、排版（全形态硬性）

- **章节标题一律 `#`（H1）**，不用 `##` / `###`。
- **全篇禁用 `---` 分割线**，区块之间靠 `#` 标题分隔。
- **不写 YAML Frontmatter、不打 Obsidian 双链 `[[ ]]`**（历史残留，Notion 端不消费）。
- **媒体占位符**：`🖼️ [图片占位：…]` / `🎞️ [GIF 占位：…]` / `📊 [图表占位：…｜图注须标注…]`。一篇 5–12 个，不泛滥。

### 空行规则（强制 · 直接决定 Notion 里的分块与留白）

Markdown 的**单个空行**只是分块，到 Notion 不留痕；**连续两个空行**才会被 `notion_upload.py` 转成一个**空段落块**，也就是读者真正看得见的留白（文档首尾不落空块）。据此：

- **正文章节的 `#` 标题之下空两行**（产生一个空块），再接内容。
- **非正文的 `#` 标题之下只空一行**（不产生空块）：文档主标题、`# 嘉宾背景`、`# 核心观点`、`# 写在最后`。
- **对话形态：说话人标签之前空两行**，标签与其发言之间空一行。
- **同一段落层级的相邻段之间空一行**。不空行会被合并成同一个段落块，在 Notion 里粘成一大坨。
- 空行最多连续两个，不要堆更多。

### 说话人标签（仅对话 / 访谈形态）

- 格式：**`emoji + 空格 + 姓名`，不加冒号、不加任何标点**，标签独占一行，内容从下一行开始。
- **emoji 固定约定**：主持人 `🎙️`、男性嘉宾 `👱‍♂️`、女性嘉宾 `👩`。同一人全篇不变。
- **不得凭姓名推断性别**。性别须有可核实的依据（本人公开自述、官方页面、节目方称呼）；无从判断时用中性的 `🎙️` 或 `👤`。
- 多位主持人若自动字幕无法区分，**统一标注为「主持人」并在文末「本文来源」里声明**，不要凭猜分配。

### 两种插入物的分工

- **`>` 引用块**：金句、嘉宾引用的推文 / 数据、英文原句 + 中文翻译，以及未走并入协议时的外部素材「📎 延伸」块。
- **`💡` 单行**：**编者的提炼**，紧跟对应内容之后，一两句点破机制或反差。**克制使用**——逐字稿全篇 1–3 处；图文精读可略多（含「说人话」保底句）。

---

## 三、去 AI 味（强制自检）

- **尽量不用破折号 `——`**：破折号密集是最典型的 AI 痕迹。**例外**：对话被打断的半句可用 `——` 收尾。
- **少用「不是…而是…」**，一篇两三次就够。
- **禁用包装词**：值得一提的是、不难看出、综上所述、换句话说、某种程度上、令人瞩目、难得地。
- **禁用空转连接**：「首先 / 其次 / 最后」式机械罗列、「不仅…而且…」滥用、排比句轰炸。
- **原则**：用具体事实代替形容词；能删的过渡句直接删；嘉宾原话可口语，编辑旁白要书面克制。

---

## 四、各形态如何接入

| 形态 | 形态主体 | 与共有壳的衔接 |
|---|---|---|
| **对话体逐字稿**（模式 B） | 按原始时间线的对话章节，每节一个 `#` 标题 | 完全按共有壳。细则见 `dialogue_transcript.md` |
| **图文精读**（模式 A） | 按主题重构的叙事章节 | 共有壳之外**保留模式 A 专有的**：摘要钩子、封面 Recraft Prompt、导语中的「📍 本文路线」路线条、正文中的一句话路标与「说人话」保底句、系列回链。**原「结尾收束段」统一改称 `# 写在最后`**，双锚点关系不变（导语 + 写在最后）。细则见 `illustrated_deepdive.md` |
| **单人演讲实录** | 按幻灯片锚定的第一人称分节 | 无说话人标签；其余全按共有壳 |
| **观察 / 观点稿**（模式 C） | 中心判断线 + 分判断 | 共有壳照用；「嘉宾背景」按信源情况可省。细则见 `observation_commentary.md` |

**共有壳的区块名以本文件为准**。历史稿件里的旧名（如「视频价值」「内容与价值概括」对应现在的「导语」，「结尾收束段」对应现在的「写在最后」）不必回头改，但新稿一律用新名。
